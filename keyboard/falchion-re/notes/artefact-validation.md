# Phase 8 artefact validation

Generated by `tool/verify_patched_region.py`. Do not edit by hand.

> **NOT CLAIMED. This validates structure only. Nothing here is evidence that any image boots, and no device was accessed.**

Target: the USB product string at region+`0x882`, `ROG FALCHION ACE HFX` → `UNTESTED FALCHION FW`.

## installed

- artefact `installed-1.59-application_productstring_UNTESTED_96f6157868da.bin`
- rollback `installed-1.59-application_noop_UNTESTED_fc6128ab089e.bin` (sha256 `fc6128ab089e4fd7…`)
- compressed region flash `0x3f380..0x3f780`, 254 tokens, consumed `0x3fd`
- patched literals at stream `0x31b..``0x32e` = flash `0x3f69b..``0x3f6ae`
- propagation reads: **0**
- decoded diff: **19 bytes** changed, 1 coincided, **0 outside the intended span**

| check | result |
|---|---|
| the artefact filename carries UNTESTED | PASS |
| the rollback build is byte-identical to the source | PASS |
| the patch did not move the compressed region | PASS |
| the original region still decodes to its declared length | PASS |
| the patched region decodes to the same declared length | PASS |
| the patched stream consumes identically | PASS |
| the token structure is unchanged | PASS |
| every patched output byte is a LITERAL in the token stream | PASS |
| the literal stream offsets are contiguous | PASS |
| NO later back-reference reads from the patched output range | PASS |
| NO decoded byte outside the intended span changed | PASS |
| every differing byte is accounted for by the replacement | PASS |
| the original decoded bytes are the expected string | PASS |
| the patched decoded bytes are the replacement string | PASS |
| the replacement preserves length and character class | PASS |
| the record chunked-CRC matches an independent recomputation | PASS |
| the application word-sum matches an independent recomputation | PASS |
| the word-sum's covered range contains the record CRC field | PASS |
| every layout check that passed on the source still passes | PASS |

## vendor

- artefact `vendor-1.00.58-full_productstring_UNTESTED_fda342b393a7.bin`
- rollback `vendor-1.00.58-full_noop_UNTESTED_6d410ee0a54f.bin` (sha256 `6d410ee0a54f640b…`)
- compressed region flash `0x3f354..0x3f754`, 253 tokens, consumed `0x3fe`
- patched literals at stream `0x31b..``0x32e` = flash `0x3f66f..``0x3f682`
- propagation reads: **0**
- decoded diff: **19 bytes** changed, 1 coincided, **0 outside the intended span**

| check | result |
|---|---|
| the artefact filename carries UNTESTED | PASS |
| the rollback build is byte-identical to the source | PASS |
| the patch did not move the compressed region | PASS |
| the original region still decodes to its declared length | PASS |
| the patched region decodes to the same declared length | PASS |
| the patched stream consumes identically | PASS |
| the token structure is unchanged | PASS |
| every patched output byte is a LITERAL in the token stream | PASS |
| the literal stream offsets are contiguous | PASS |
| NO later back-reference reads from the patched output range | PASS |
| NO decoded byte outside the intended span changed | PASS |
| every differing byte is accounted for by the replacement | PASS |
| the original decoded bytes are the expected string | PASS |
| the patched decoded bytes are the replacement string | PASS |
| the replacement preserves length and character class | PASS |
| the record chunked-CRC matches an independent recomputation | PASS |
| the application word-sum matches an independent recomputation | PASS |
| the word-sum's covered range contains the record CRC field | PASS |
| every layout check that passed on the source still passes | PASS |

