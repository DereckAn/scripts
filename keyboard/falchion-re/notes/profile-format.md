# Falchion Ace HFX — the settings / profile format

Generated by `tool/map_profile_format.py`. Static and offline: no device was accessed and no command is constructed here.

## The question that was asked

Lighting configuration persists across hosts because the whole 0x81c profile block, lighting slots included, is written to the external settings store by the vendor persistence path and read back at boot before any host has enumerated the device. The sentence is CORRECT in substance and imprecise in two places. First, the write is not part of the command handler: the handler only sets a command byte, and a state machine in another context performs the checksum, the write request and the retries. Second, 'the profile region' is five regions, not one, and lighting rides in the profile-settings region at 0x2000 + profile*0x1000, not in the 0x320000 keymap region. The reason the SELECTED profile also survives is separate: the current profile index is byte +6 of a 16-byte header kept in a wear-levelled A/B store at 0x1c000..0x20000.

## Runtime blocks

| address | size | block |
|---|---|---|
| `0x1801e6d0` | `0x10` | device_header |
| `0x180202d8` | `0xd84` | keymap_layer0 |
| `0x1802105c` | `0xd84` | keymap_layer1 |
| `0x18021de0` | `0x81c` | profile_block |
| `0x180225fc` | `0x664` | macro_block |
| `0x18024f0c` | `0x20` | global_block |
| `0x1801fef8` | `0x3e0` | block_d |

The four middle blocks are contiguous, which is why the profile block is sometimes reached as 'layer 2' of the keymap bank.

## Stored map

| region | range | formula | record |
|---|---|---|---|
| profile_settings | `0x2000`..`0x8000` | `0x2000 + profile * 0x1000` | `0x81c` |
| small_store | `0x1c000`..`0x20000` | `four fixed 4 KiB banks, two A/B pairs` | — |
| macros | `0x20000`..`0x320000` | `0x20000 + profile * 0x80000 + slot * 0x1000` | `0x664` |
| keymap_banks | `0x320000`..`0x338000` | `0x320000 + profile * 0x4000 + layer * 0x1000` | `0xd84` |
| block_d | `0x340000`..`0x346000` | `0x340000 + profile * 0x1000` | `0x3e0` |

## The profile block

`0x18021de0`, `0x81c` bytes.

| offset | size | field | confidence | detail |
|---|---|---|---|---|
| `+0x000` | `0x2` | checksum_a | observed | sum16(blob+2, 0x4b0) masked with (profile \| 0xfff0) |
| `+0x002` | `0x2` | flags | observed | bit 15 marks region B valid; the save path also sets 1 << lighting_slot for slots below 10 |
| `+0x004` | `0xd0` | lighting_slots | observed | 10 variable-length records; sizes [15, 27, 38] |
| `+0x0d4` | `0x1ee` | key_table_layer0 | observed | 247 uint16 plus padding; the initialiser writes the identity map and 0xa0d3 at entry 0xe4 |
| `+0x2c2` | `0x1ee` | key_table_layer1 | observed | the same shape; the initialiser leaves it zero |
| `+0x4b0` | `0x2` | pair_4b0 | unresolved | FUN_18000420 writes 0x14 and a bitfield here; role not recovered |
| `+0x4b2` | `0x46` | selectable_rows | observed | 5 rows of 14 bytes copied verbatim from ROM 0x1801bfca; FUN_18007030 treats a non-zero first byte as selectable |
| `+0x4f8` | `0x2` | version_stamp | observed | copied from ROM 0x1801bfbc; not covered by either checksum |
| `+0x4fa` | `0x2` | checksum_b | observed | sum16(blob+0x4fc, 0x2c0), recomputed only when flags bit 15 is set |
| `+0x4fc` | `0x2c0` | region_b | unresolved | covered by checksum_b; contents not decoded |
| `+0x7bc` | `0x60` | tail | unresolved | covered by NEITHER checksum; contents not decoded |

### The lighting record

| offset | field | detail |
|---|---|---|
| `+0x00` | effect | 0x0a, 0x0f, 0x14, 0x1e, 0x1f or 0x3c by default |
| `+0x01` | brightness | default 100 |
| `+0x02` | unnamed | 0, or (profile == 0) for slots 3, 6, 8 and 9 |
| `+0x03` | minus_one_a | 0xff |
| `+0x04` | minus_one_b | 0xff |
| `+0x05` | r | request byte 4 |
| `+0x06` | g | request byte 5 |
| `+0x07` | b | request byte 6 |

Slots and their offsets:

| slot | offset | size |
|---|---|---|
| 0 | `+0x004` | 15 |
| 1 | `+0x013` | 15 |
| 2 | `+0x022` | 15 |
| 4 | `+0x031` | 38 |
| 5 | `+0x057` | 38 |
| 3 | `+0x07d` | 15 |
| 6 | `+0x08c` | 15 |
| 7 | `+0x09b` | 27 |
| 8 | `+0x0b6` | 15 |
| 9 | `+0x0c5` | 15 |

## The keymap bank

`0x180202d8 + layer * 0xd84`, sum16(bank + layer*0xd84 + 2, 0xd82) in the bank's first halfword, which lives in record 0's first four bytes because the default setter never writes them

| offset | detail |
|---|---|
| `+0x04` | 0x00 on layer 0, 0xff on layer 1 |
| `+0x06` | bit 7 override, bits 0..2 rapid trigger press, 1..6, default 2 |
| `+0x07` | bit 7 override, bits 0..2 rapid trigger release, 1..6, default 2 |
| `+0x08` | uint16, bit 15 override, bits 0..6 actuation, 1..40, default 10 |
| `+0x0a` | uint16 remap target; the 0x51/0x21 handler writes it |

## The global block

`0x18024f0c`, 32 bytes: six packed 32-bit words, one per profile, and a `0xa9b8` marker at `+0x18`.

| bits | field | range | default | evidence |
|---|---|---|---|---|
| 6..8 | function_index | 0..4 | 0 | cycled 0..4 by FUN_18007030, which then persists the whole block |
| 9..15 | actuation | 1..40 | 10 | FUN_18006d3c raises while <0x28 and lowers while >1; the boot repair writes +0x1400, i.e. 10 |
| 17..19 | rapid_trigger_release | 1..6 | 2 | FUN_1800f7fe resets the field to 2 when (v)-1 > 5 |
| 20..22 | rapid_trigger_press | 1..6 | 2 | FUN_1800f7fe resets the field to 2 when (v)-1 > 5 |

## The wear-levelled store

- mount: FUN_1800e4ea, from CandidateB_Main
- read: FUN_1800e64e; on a never-written item it fills the caller's buffer with 0xff and logs FR_fail
- write: FUN_1800e6d6 synchronously, FUN_1800e7ee to stage
- compaction: FUN_1800e3c8 copies each item's newest record to the other bank, zeroes the old bank's first four bytes and flips the selector
- validation: a bank is live when its first four bytes are neither 0xffffffff nor 0. There is no magic and no checksum at this layer.

| group | bank A | bank B | item | record | offset | capacity |
|---|---|---|---|---|---|---|
| 0 | `0x1c000` | `0x1d000` | `0x1801e93c` | `0x10` | `0x0` | 64 |
| 0 | `0x1c000` | `0x1d000` | `0x1801e96c` | `0x20` | `0x400` | 32 |
| 0 | `0x1c000` | `0x1d000` | `0x1801e9a0` | `0x1` | `0x800` | 2048 |
| 1 | `0x1e000` | `0x1f000` | `0x1801e9c0` | `0x2` | `0x0` | 256 |

## Load

- **boot** — CandidateB_Main calls FUN_1800e4ea, which mounts the wear-levelled store: for each of two groups it reads the first four bytes of both banks, picks the one that is neither 0xffffffff nor 0, and scans that bank backwards for the newest record of each item.
- **header** — FUN_18000d56's init state reads item 0 back and compares its first word against the firmware's own version word at 0x1801e6d0. EQUAL -> adopt the stored header, including the current profile index at +6. DIFFERENT -> keep only what still agrees and re-stage both items, which is the migration step.
- **global** — item 1 restores the 32-byte global block. The actuation field is then range-checked and repaired to 10 in place.
- **profile** — a two-byte read of 0x2000 + profile*0x1000 tests for 0xffff; if it is erased the defaults path runs, otherwise the whole 0x81c block is read and checksum A is recomputed and compared against the stored value masked with the profile index.
- **keymaps** — for layer 0 then 1: a two-byte read tests for 0xffff, then 0xd84 bytes are read and sum16(bank+2, 0xd82) is compared against the bank's first halfword.
- **block_d** — the same shape at 0x340000 + profile*0x1000, 0x3e0 bytes, sum16(buf+2, 0x3de).
- **failure** — every mismatch takes the same route as an erased block: the defaults are rebuilt in RAM (FUN_1800072c for the key tables, FUN_18000466/FUN_18000584 for the per-key records, FUN_180005c6 for block D, FUN_1800075a for the lighting slot) and a diagnostic string is logged. There is no factory image to fall back to and no second copy is consulted.

## Save

- **stage** — the device header and the global block are handed to FUN_1800e7ee, which copies them into the wear-levelled store's staging buffers and marks them dirty.
- **profile** — checksum A is recomputed over blob+2 for 0x4b0 bytes and stored masked with (profile | 0xfff0); checksum B is recomputed over blob+0x4fc for 0x2c0 bytes only when flags bit 15 is set; then the WHOLE 0x81c block is written verbatim from 0x18021de0.
- **macros** — FUN_18007da4 recomputes sum16(record+8, record[7]) for each of the four 0x198 records and stores it at record+0x19a — two bytes into the next record's header, and in the block's four-byte tail for the last — then writes all 0x664 bytes.
- **keymaps** — for layer 0 then 1: sum16(bank + layer*0xd84 + 2, 0xd82) is stored in the bank's first halfword and the whole 0xd84 bytes are written.
- **block_d** — sum16(buf+2, 0x3de) into the first halfword, then 0x3e0 bytes.
- **retry** — each stage retries up to three times before giving up; the vendor response is sent only after the last stage completes, which is why the historical capture recorded the reply arriving about 220 ms late.

## The Armoury Crate decode, field by field

The snapshot is profile 3, which is what makes it usable as a Rosetta stone: two of the firmware's per-profile ROM defaults are indexed by exactly that number.

| Armoury Crate field | value | blob | offset | confidence | evidence |
|---|---|---|---|---|---|
| `lighting.keyboard.effectID` | 8 | profile block | slot index -> +0x0b6 | **exact** | the ROM per-profile default lighting slot for profile 3 is 8, read from 0x1801bfbe+3. the decode's effectID is the firmware's slot index, not a field inside the record |
| `lighting.keyboard.brightness` | 100 | lighting slot record | +0x01 | **exact** | every one of the ten default-setter cases writes 100 to the record's second byte. range is not proven from the setter; only the default is |
| `lighting.keyboard.pattern.singleColor` | [0, 0, 255] | lighting slot record | +0x05..+0x07 | **exact** | the ROM per-profile colour for profile 3 is [0, 0, 255], read from 0x1801c010+3*3; the 0xfa/1/0 handler writes request bytes 4,5,6 to the same three record bytes. |
| `lighting.keyboard.direction` | -1 | lighting slot record | +0x03 | **structural** | the default-setter writes 0xff, the byte-width form of -1. 0xff is also written to +0x04, so which of the two is direction is not established |
| `lighting.keyboard.random` | -1 | lighting slot record | +0x04 | **structural** | the default-setter writes 0xff, the byte-width form of -1. same ambiguity as direction |
| `lighting.keyboard.speed` | 1 | lighting slot record | +0x02 | **unmatched** | the default-setter writes 0 for slots 0-2 and (profile == 0) for slots 3,6,8,9; the decode holds 1 for profile 3, which neither default produces. |
| `lighting.customPattern` | 7 entries | lighting slot record | slot 7, +0x05..+0x16 | **unmatched** | slot 7's default-setter writes six RGB triples; the decode has seven entries. six is not seven, so no correspondence is claimed |
| `performance.pollingRate` | 3 | - | - | **unmatched** | no field in any block recovered here carries it, and log 124 searched from the other end and found no consumer either: no index-to-Hz table in any of the four images, a prescaler ladder built entirely from immediates, no rate in the mailbox and a static descriptor bInterval. the two negatives are independent — this one is about the stored blocks, log 124's is about the consumers |
| `button.analogTrigger.actuation` | 10 | global block word | bits 9..15 | **exact** | the boot repair writes +0x1400, i.e. 10; FUN_18006d3c bounds the field to 1..40. |
| `button.analogTrigger.rapidTriggerPress` | 2 | global block word / per-key record | bits 20..22 / record +0x06 | **exact** | FUN_1800f7fe resets both to 2 when the value minus one exceeds 5, so the default is 2 and the range 1..6. |
| `button.analogTrigger.rapidTriggerRelease` | 2 | global block word / per-key record | bits 17..19 / record +0x07 | **exact** | the same reset, on the neighbouring field. |
| `lever.currentFunctionId` | 0 | global block word | bits 6..8 | **structural** | the ROM per-profile default is 0; FUN_18007030 cycles the field 0..4. the values agree, but both are zero, which is weak evidence; and the same field could equally be a lighting-effect index. Both readings are recorded and neither is chosen |
| `lever.functionStatusList` | 5 entries | profile block | +0x4b2 | **structural** | 5 rows of 14 bytes, each gated on a non-zero first byte; the counts and the 0..4 index agree. row contents are not decoded, so the correspondence is on count and index range only |
| `button.keyboardButton` | 136 entries = 68 keys x 2 layers | profile block key tables / keymap banks | +0x0d4 and +0x2c2 | **structural** | the firmware keeps 2 layers, 247 uint16 per layer, and 75 per-key records per bank; none of 68, 136 or 189 equals any of those. the layer count matches and the entry counts do not, so no per-key mapping is claimed |
| `button.keyboardButton[*].actuation` | 10 | per-key record | +0x08 bits 0..6 | **exact** | bit 15 selects the per-key value over the global one; the field is bounded to 1..40 by the same comparison. |

## Persisted versus RAM-only

| section | verdict | detail |
|---|---|---|
| lighting | **persisted** | the lighting slots live at +0x04..+0xd3 of the profile block, inside checksum A's coverage, and the whole block is written to 0x2000 + profile*0x1000 by the save path. |
| key mappings | **persisted** | two forms: the resolved per-layer uint16 tables inside the profile block, and the 0x20-byte per-key records in the two 0xd84 banks written to 0x320000 + profile*0x4000 + layer*0x1000. |
| performance (actuation, rapid trigger) | **persisted** | the global 32-byte block is staged into the wear-levelled store on every save and restored at boot; the per-key overrides ride in the keymap banks. |
| current profile index | **persisted** | byte +6 of the 16-byte device header, item 0 of the wear-levelled store. |
| macros | **persisted** | four 0x198 records written to 0x20000 + profile*0x80000 + slot*0x1000. |
| block D | **persisted** | 0x3e0 bytes per profile; contents not decoded. |
| per-key Hall calibration | **RAM-only** | log 121: rebuilt from the same defaults every boot, and no storage path touches the calibration block. |

## Unresolved

- **storage_medium** — unchanged from log 111. The format is recovered; the part the DMA path talks to is still not identified and is not named here.
- **region_b** — 0x2c0 bytes at +0x4fc are checksummed and never decoded.
- **tail** — 0x60 bytes at +0x7bc are covered by neither checksum and never decoded.
- **block_d_contents** — block D is proven persisted per profile and its default setter is known; what it holds is not recovered.
- **polling_rate** — no recovered field carries it, and log 124 found no consumer for one anywhere in the preserved images.
- **numeric_overlap** — the stored map's low regions numerically overlap the bootloader's application region 0x10000..0x7c000. Log 111's 'disjoint address ranges' held for the three erase targets it traced and does NOT generalise. Whether the two are the same address space cannot be settled while the medium is unidentified.

## Checks

- PASS — both preserved dump hashes match the allowlist
- PASS — the ten lighting slots exactly fill the gap before the key tables (0x04 + 0xd0 == 0xd4)
- PASS — two key-table layers end where checksum A's coverage ends (0xd4 + 2*0x1ee == 0x4b0)
- PASS — checksum A covers exactly flags, lighting and the key tables (+2 .. +0x4b2)
- PASS — the selectable rows end where the version stamp begins
- PASS — the version stamp and checksum B sit between the two covered runs
- PASS — checksum B's run ends inside the block, leaving an uncovered tail (0x60 bytes uncovered)
- PASS — the key table's entry count and byte stride agree (247 uint16 == 0x1ee bytes)
- PASS — the profile block begins exactly after the two keymap banks (0x180202d8 + 2*0xd84 == 0x18021de0)
- PASS — the macro block begins exactly after the profile block
- PASS — four macro records plus a four-byte tail fill the macro block (4*0x198 + 4 == 0x664)
- PASS — every macro checksum slot lies inside the block (record i's sum16 is stored at i*0x198+0x19a, which is two bytes into the NEXT record's header and, for the last record, in the four-byte tail)
- PASS — every request primitive's opcode is decoded from its own instruction and matches (offsets +10/r2, +10/r2, +10/r2, +12/r4, +12/r4, +12/r4)
- PASS — the six primitives carry six distinct opcodes
- PASS — the idiom is searched for, not read at one fixed offset (the six do not share a single offset/register pair)
- PASS — the opcode decoder finds nothing in bytes that do not carry the idiom (anti-vacuity for the search)
- PASS — each wear-levelled group's two banks are one 4 KiB apart
- PASS — each group's items fit inside one bank
- PASS — every item's declared region length is capacity x record length
- PASS — every item names the group that lists it
- PASS — the two staged blocks are named by their callers, not by the descriptors (the descriptors point at private staging buffers; FUN_1800e7ee copies the caller's block into them)
- PASS — the ROM colour for profile 3 equals the decode's single colour ([0, 0, 255] from 0x1801c010)
- PASS — the ROM lighting slot for profile 3 equals the decode's effectID (slot 8)
- PASS — every per-profile default slot is one the setter implements ([4, 2, 6, 8, 9, 3])
- PASS — the ROM row count equals the decode's function-list length (5)
- PASS — the factory-reset word reproduces every field's default (0x241410)
- PASS — no two global-word fields share a bit
- PASS — the checksum model wraps at 16 bits rather than saturating (sum16(0xff * 0x1000) == 0xf000)
- PASS — the stored checksum carries the profile index in its low nibble
- PASS — a block stamped for one profile does not validate in another (anti-vacuity for the stamp)
- PASS — the five stored regions do not overlap each other
- PASS — the stored map DOES numerically overlap the bootloader's application region, so log 111's disjointness does not generalise (['small_store', 'macros'])
- PASS — log 111's own three erase targets remain above the application region, so its narrower claim still holds
- PASS — at least one Armoury Crate field is left unmatched (a table with no unmatched rows would mean fields were forced)
- PASS — every match row cites evidence
- PASS — polling rate stays unmatched
- PASS — the persistence table records both verdicts
- PASS — the host-independence answer both confirms and corrects the sentence it was asked about
- PASS — the storage medium stays unresolved
