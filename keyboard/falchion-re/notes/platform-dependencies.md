# Platform dependencies and the final gate (Phase 5G)

Generated by `tool/map_platform_dependencies.py`. Do not edit by hand.

## 5G findings

| area | finding | confidence | verified |
|---|---|---|---|
| watchdogs | both magic-key blocks are written a cleared control word on the reset path | strongly-inferred | bytes |
| watchdogs | the MMIO census sees ONE writer, and the census is wrong — a second cluster reaches the same blocks through a call-through base selector it cannot attribute | observed | listing |
| watchdogs | block 0x40008000 IS fed periodically, on the tick chain | observed | listing |
| watchdogs | the NMI handler conditionally acknowledges and re-arms block 0x40008000, then escalates to a system reset | strongly-inferred | listing |
| watchdogs | the escalation limit is ONE: the first acknowledged NMI also triggers SYSRESETREQ | strongly-inferred | bytes |
| watchdogs | selector 1, and therefore block 0x40009000, is never passed by any live path | observed | xref |
| watchdogs | the usbd_wdt task does NOT service any hardware watchdog | observed | xref |
| clocks | the reset-path init reads 0x45000000 and 0x4500000c to choose a branch, validates the stack pointer, then runs a fixed five-call chain | observed | listing |
| clocks | the reset path requires the stack pointer to lie in 0x18000000..0x18040000, and faults out otherwise | observed | listing |
| clocks | no clock frequency is established anywhere | unresolved | listing |
| clocks | the reset path writes 0x4002f000 = 3 and 0x4002f004 = 0x60021000 | observed | xref |
| faults | the NMI handler requests a system reset | observed | listing |
| faults | the HardFault handler reads CFSR, MMFAR and BFAR — diagnostics, not a hardware requirement | strongly-inferred | xref |
| faults | the vector table is 80 slots and IRQ63 is live | strongly-inferred | bytes |
| multicore | a second execution context is started and waited for at boot | observed | listing |
| multicore | the handshake token exists in exactly two places, one of them inside the 0x18038000 image | observed | bytes |
| multicore | the start routine is entry-image 0x1f50, reached by a veneer, and it manipulates 0x45000100 | strongly-inferred | bytes |
| multicore | the 0x18038000 image carries its own vector table with reset 0x180381c1 | observed | bytes |
| multicore | what the second context OWNS is not established | unresolved | xref |
| multicore | no ROM call was identified | unresolved | xref |

## Service classification

| service | class | confidence | safe idle proven |
|---|---|---|---|
| reset, clock and RAM bring-up | **must-implement** | observed | — |
| the two magic-key watchdog blocks | **must-neutralize** | strongly-inferred | — |
| the periodic tick that drives everything | **must-implement** | observed | — |
| per-key Hall sample acquisition | **unresolved** | unresolved | — |
| key-state generation from travel bytes | **must-implement** | observed | — |
| USB enumeration and the descriptor set | **must-implement** | observed | — |
| interface 0, the 8-byte boot report | **must-implement** | observed | — |
| the control endpoint | **must-implement** | observed | — |
| the second execution context at 0x18038000 | **must-neutralize** | observed | — |
| the vendor 0xFF00 configuration channel | **may-omit** | observed | yes |
| nonvolatile settings and the commit path | **may-omit** | strongly-inferred | yes |
| RGB / LampArray lighting | **unresolved** | unresolved | — |
| interface 2 and 3, media and NKRO reports | **may-omit** | strongly-inferred | yes |
| the fault logger and debug strings | **may-omit** | strongly-inferred | yes |
| the NMI handler's system reset | **must-neutralize** | observed | — |
| the vendor RTOS | **may-omit** | strongly-inferred | yes |
| the actual clock configuration | **unresolved** | unresolved | — |

## Blockers for a first typing prototype

- **per-key Hall sample acquisition** — no function in either analysed image writes the per-key travel array from a hardware register, and the buffer is reachable only by dereferencing a pointer cell that something fills at runtime. The producer is outside the analysed set. The second execution context at 0x18038000 is a CANDIDATE owner and is not evidence.
- **RGB / LampArray lighting** — the LED driver is unidentified, so its idle polarity is unknown and no shared clock, pin or controller initialisation could be inspected. Proving safe omission requires identifying the consumer of the 306-byte frame at 0x1802505e.
- **the actual clock configuration** — no crystal value, PLL multiplier or divider with a stated unit appears in the recovered code, and no register map exists for the blocks the sequence writes. Any Hz figure would have to come from the silicon or from measurement, neither of which this project has.

## Prototype status

| requirement | status |
|---|---|
| reset/clock/RAM | sequence preserved; FREQUENCY UNRESOLVED |
| watchdog policy | known, and it is NOT just a disable: reset clears both, the /8 tick feeds 0x40008000, and NMI acknowledges it and resets after one strike (log 114) |
| Hall acquisition | BLOCKED — the producer is not recovered |
| key-state generation | recovered and executable |
| USB keyboard-IN | recovered end to end |

## Checks

- PASS — every service has a classification from the closed set (17 services)
- PASS — every service cites at least one piece of evidence
- PASS — every UNRESOLVED service names an exact evidence boundary (hall_acquisition, rgb, clock_frequency)
- PASS — no service is classified may-omit without a proven safe idle state (omission requires proof, not silence)
- PASS — a service whose idle state is unproven is unresolved, not omittable (RGB is the case this rule exists for)
- PASS — the Hall acquisition is recorded as a blocker, not an omission
- PASS — the gate reads the five upstream models rather than restating them (hall-actuation.json, nonvolatile-writes.json, rgb-lamparray.json, scan-pipeline.json, usb-routing.json)
- PASS — every finding carries a confidence and a source
- PASS — all four areas are covered by findings
- PASS — no clock frequency is claimed
- PASS — the vector-table extent stays strongly-inferred
- PASS — the usbd_wdt lead is recorded as NOT panning out
- PASS — the model names all THREE watchdog access paths (reset-path disable, periodic feed, NMI acknowledge and escalate)
- PASS — the model records that two of the three are census-invisible (the base arrives from a call-through selector, so a per-function census cannot attribute them)
- PASS — the withdrawn claims are recorded, not silently dropped
- PASS — the model states the blocks ARE fed
- PASS — the model records that only the first block is touched after reset
- PASS — the watchdog conclusion is inference, not identification
- PASS — the second context is must-neutralize, because main spins on it
- PASS — every class is populated, so the gate is a real partition (must-implement=6; must-neutralize=3; may-omit=5; unresolved=3)
