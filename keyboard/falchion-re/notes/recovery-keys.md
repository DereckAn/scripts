# The bootloader's recovery key combination

**Status: gate G1 PARTIAL.** Generated by `tool/map_recovery_keys.py`. Do not edit by hand.

> Offline analysis of preserved images. No device was accessed. This resolves an analysis gate and authorises nothing live.

## The answer

| | group | position | code | key | identity |
|---|---|---|---|---|---|
| **DOWN** | 0 | 5 | `0x25` | **8** | observed |
| **DOWN** | 0 | 7 | `0x23` | **6** | observed |
| **DOWN** | 4 | 8 | `0xe8` | **UNKNOWN vendor code 0xe8** | unresolved |
| must be UP | 0 | 6 | `0x24` | **7** | observed |

Because the poll compares for **exact equality**, every other position in groups 0 and 4 must be released. Groups 1, 2 and 3 are never read.

## The scan mechanism

**the same muxed analog converter path the second execution context uses — NOT digital GPIO and NOT a row/column matrix**

- strobe `0x4001b000`
- data-out `0x40018000`
- data-in `0x40019000`

write a 16-bit word to the data-out register, strobe with 0x7c then 0, read the data-in register, store the result — the identical sequence and the identical strobe value as FUN_1803ae58 in the 0x18038000 image

each key is its own analog channel. There is no matrix, so there is no ghosting and no mutually exclusive pair: any set of keys can be read simultaneously.

A correction worth recording: 0x00005272 is `msr primask,r0`, so the poll's 1/0 calls are an interrupt mask, NOT a scan enable.

## Geometry

5 words of 15 bits; bit b of word g is linear index g*15 + b — 75 positions. a position's bit is SET when its level is non-zero AND below 0x1300; zero or at/above that reads as up. Bounds from `cmp r7,#0xf` inner and `cmp r5,#0x5` outer, in the packer.

## How the codes are known to be HID usages

At `0x180063f6` the firmware applies `sub #0xe0 / cmp #7 / and #7 / 1 << n / or into the report's modifier byte`. That is the USB boot keyboard modifier rule, and it only makes sense if the map's values are usage IDs. USB HID Usage Tables, Keyboard/Keypad page 0x07: usages 0xE0..0xE7 are the eight modifiers, in that bit order.

## The application key map

| group | p0 | p1 | p2 | p3 | p4 | p5 | p6 | p7 | p8 | p9 | p10 | p11 | p12 | p13 | p14 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 0 | Backspace | = and + | - and _ | 0 | 9 | 8 | 7 | 6 | 5 | 4 | 3 | 2 | 1 | Escape | (unused/skipped) |
| 1 | \ and \| | ] and } | [ and { | P | O | I | U | Y | T | R | E | W | Q | Tab | (unused/skipped) |
| 2 | Enter | ' and " | ; and : | L | K | J | H | G | F | D | S | A | Caps Lock | (unused/skipped) | (unused/skipped) |
| 3 | Up Arrow | Right Shift | / and ? | . and > | , and < | M | N | B | V | C | X | Z | Left Shift | (unused/skipped) | (unused/skipped) |
| 4 | Insert | Delete Forward | Page Up | Page Down | Right Arrow | Down Arrow | Left Arrow | Right Control | UNKNOWN vendor code 0xe8 | Right Alt | Spacebar | Left Alt | Left GUI | Left Control | (unused/skipped) |

## The third key

Code `0xe8` occurs once, at group 4 position 8, in both layers. Its neighbours are `0xe4` (Right Control), `0xe6` (Right Alt).

outside 0xe0..0xe7, so the modifier rule does not apply, and outside 0xe9..0xeb, which is the only other range the report builder handles.

**Inference (strongly-inferred):** a keyboard-local key that is never reported to the host and sits between Right Control and Right Alt in the bottom row. On this product that position is the Fn key.

*What would settle it:* a recovered consumer of the code that switches the key map's layer, or a capture showing the key produces no HID traffic while changing other keys' output.

## The residual: group correspondence

**Proven: False.** the bootloader contains no key map and never names a key, so the claim that its group index equals the application's rests on both reading the same physical channel grouping, corroborated above but not proven from the images.

- the packer's 'not 0x49' mask disables position 14 in all five groups, and the application's map has an unused code at position 14 in all five groups — holds: **True**. consistent, but symmetric across groups, so it does not fix the group permutation.
- the packer's 0x44 'D' mask disables exactly (group 2, position 13) and (group 3, position 13), and the application's map holds a skip code at both — a real key at either would contradict the identity mapping — holds: **True**. this one DOES depend on the group permutation, so it is genuine corroboration for groups 2 and 3.

*What would settle it:* a recovered table in either image that maps a scan channel to a group index, or an observation of the bitmap with a single known key held..

## Timing

AFTER container selection (0x00002af0 at 0x00007ed6 chooses the entry) and BEFORE all three remaining boot gates: the entry-constant compare, the 0x20000ffc one-shot flag (FUN_00002a44) and the checksum (FUN_000026d0). It is the FIRST gate that can block the boot.

- delay unit: **microseconds** — FUN_000048dc computes (core_clock / 1000000) * argument and then busy-loops. Dividing a measured clock by 1e6 makes the argument microseconds BY CONSTRUCTION, so the unit holds without knowing the clock's numeric value — which is still unresolved.
- warm-up: 100 samples, discarded
- match window: 100 samples
- hold required: 31 consecutive
- delays alone: about 200 ms

the first 100 samples are discarded, so the combination must already be held when the poll starts and must stay held into the second phase for at least 31 consecutive samples. In round numbers: hold the keys from power-on through roughly the first fifth of a second. The delays alone total about 200 ms; the scan itself adds 200 unmeasured conversions on top, so the true window is longer than 200 ms and is not bounded here.

both loops run unconditionally when no key is held, so EVERY power-on spends this window in the poll.

## Plausibility

**physically holdable.** the scan is per-key analog, one channel per key, so there is no matrix and no ghosting; two positions in the same group are separate bits of one word, not two ends of one channel

the two number-row positions are two apart with a third position between them that must stay released, so the combination is 'press the outer two of three adjacent keys'. That is deliberate-looking and is easy to do with two fingers, but it is not a combination anyone would strike by accident.

## Answers

### What does the bootloader's scan actually sample?

The same muxed analog converter path the second execution context uses: strobe 0x4001b000, data-out 0x40018000, data-in 0x40019000, with the identical write-strobe-readback idiom and the identical 0x7c strobe value. It is NOT digital GPIO and NOT a row/column matrix. The bootloader carries its own copy of the driver and does not use the second context.

- confidence: **observed**
- basis: the census attributes those three registers to FUN_000067a4, and its literal pool holds all three
- evidence: this log step 1, log 119 step 4

### What does each bit of the matched buffer mean?

The buffer is five words of fifteen bits. Bit b of word g is linear scan index g*15 + b, set when that position's level is non-zero and below 0x1300. The dimensions come from the packer's own loop bounds, and they match the application's 5 x 15.

- confidence: **observed**
- basis: `cmp r7,#0xf` and `cmp r5,#0x5` in FUN_0000350c, with the bit-set/bit-clear stores through the same word index
- evidence: this log step 2, log 110

### Which positions does the pattern require?

Three pressed — group 0 positions 5 and 7, group 4 position 8 — and, because the compare is exact equality, every other position in groups 0 and 4 released, including group 0 position 6 between the two pressed ones. Groups 1, 2 and 3 are never read.

- confidence: **observed**
- basis: 0xa0 has bits 5 and 7; 0x100 has bit 8; the poll uses `cmp` for equality and never loads +0x4, +0x8 or +0xc
- evidence: this log step 2, log 101 step 3

### Which physical keys are they?

Two are named with a complete chain: group 0 position 5 is HID usage 0x25, the '8' key, and group 0 position 7 is usage 0x23, the '6' key; the position between them that must stay released is usage 0x24, the '7' key. The third, group 4 position 8, is vendor code 0xe8, which is NOT a HID usage and is never emitted to the host.

- confidence: **observed**
- basis: the application's key map at 0x1801c940 indexed by group*15+position, with the firmware's own modifier rule proving the values are HID usage IDs
- evidence: this log step 3

### What is the third key?

Not resolved by name. Code 0xe8 is the only non-HID code in the map, occurs exactly once, in both layers, and sits between Right Control and Right Alt in the bottom row — the Fn position on this product. That is an inference from layout and from the absence of any emit path, not a recovered name.

- confidence: **strongly-inferred**
- basis: the code is outside both ranges the report builder handles, and its neighbours in the map are 0xe4 and 0xe6
- evidence: this log step 3

### When does the poll matter, and for how long?

It runs on every boot, after container selection and before all three remaining boot gates, so it is the first thing that can block a boot. The first 100 samples are discarded and a match needs 31 consecutive samples in the second 100. The delays alone are about 200 ms; the scans add an unmeasured amount on top.

- confidence: **observed**
- basis: FUN_00007ec8's call order, and FUN_000048dc dividing a computed core clock by 1000000, which makes the argument microseconds
- evidence: this log step 4, log 101

### Can a user hold the combination?

Yes. The scan is per-key analog with one channel per key, so there is no matrix, no ghosting and no mutually exclusive pair. The awkward part is deliberate: two number-row keys with the key between them released.

- confidence: **observed**
- basis: the converter drives one channel per position; two positions in a group are separate bits of one word
- evidence: this log steps 1 and 5

### Does this resolve risk-plan gate G1?

PARTIAL. G1 asked which state is sampled and which key positions the pattern corresponds to. The state and the positions are fully resolved. Two of the three keys are named with a complete chain; the third has a matrix coordinate and a strongly-inferred name. The group-index correspondence between bootloader and application is corroborated, not proven.

- confidence: **strongly-inferred**
- basis: the chain is complete except for two named links, each of which is reported with what would settle it
- evidence: this log step 6, notes/step7-live-experiment-risk-plan.md

## Unresolved

- **third_key_name** — Code 0xe8 has a matrix coordinate (group 4, position 8) and no recovered name. Fn is an inference from its neighbours and from having no emit path, not a finding.
- **group_correspondence** — The bootloader holds no key map. That its group index equals the application's is corroborated by the variant masks landing on non-keys, but is not proven from the images.
- **absolute_clock** — The delay's UNIT is microseconds by construction, but the core clock's numeric value is still unresolved, so the scan time inside each sample is not bounded and the true poll window is longer than the 200 ms of delays.
- **variant_byte** — The byte at 0x18010b56 selects a layout variant ('D', 'E', 'I' or other). What writes it is not traced. The recovery positions are not masked under any variant, so the answer does not depend on it.
- **down_ceiling** — A position counts as down when its level is non-zero and below 0x1300. The units of that level are unknown, so no travel distance or force is implied.
- **historical_labels** — The names here come from the published HID usage table via the firmware's own map, NOT from this repository's historical capture labels, which FINDINGS records as not a universal physical layout.

## Checks

- PASS — both preserved source hashes match the allowlist
- PASS — the bootloader slice matches its recorded hash (c244aef0a92424cc)
- PASS — bootloader_primary and the installed mirror are byte-identical (so the analysed program is the same bytes either way)
- PASS — the poll's buffer address is read from its own pool (0x18012ac8)
- PASS — a match needs 31 consecutive samples (cmp r5,#0x1e then increment)
- PASS — the bootloader's scan drives the converter trio, not GPIO (0x4001b000, 0x40018000, 0x40019000)
- PASS — the trio is the same one log 119 recovered in the second context
- PASS — the bitmap is 5 words of 15 bits (`cmp r7,#0xf` inner and `cmp r5,#0x5` outer, in the packer)
- PASS — 75 positions, matching the application's key map dimension
- PASS — the pattern decodes to exactly three pressed positions ([(0, 5), (0, 7), (4, 8)])
- PASS — exact equality also constrains the other positions in both words (27 must be released)
- PASS — the firmware applies the HID modifier rule to the map's values (0x180063f6)
- PASS — so the key map holds HID usage IDs, not arbitrary indices
- PASS — two of the three pressed keys are named with a complete chain (8, 6)
- PASS — the third is reported UNRESOLVED rather than guessed (0xe8)
- PASS — the key that must stay released is named (7)
- PASS — no variant mask touches any recovery position (so the answer does not depend on the unresolved variant byte)
- PASS — the bootloader holds no key map, which is why a residual remains
- PASS — the group correspondence is reported as corroborated, NOT proven
- PASS — both corroborating observations hold (2 observations)
- PASS — the delay's unit is established (divisor 1000000)
- PASS — the poll has exactly ONE caller (the prompt's second call site is a caller of the SCAN, not the poll)
- PASS — the poll's position in the boot order is stated
- PASS — the poll runs on every boot, not only on a key press
- PASS — the combination is physically holdable (physically holdable)
- PASS — every claim carries a known confidence label
- PASS — every claim carries a kind_basis and a citation
- PASS — the unresolved boundaries are enumerated (6 entries)
