# USB routing map (Phase 5B)

Generated by `tool/map_usb_routing.py`. Do not edit by hand.

## Descriptor set

- idVendor `0x0b05`, idProduct `0x1b7e`, bcdDevice `0x0159`
- `bNumInterfaces` 5, `bMaxPower` 500 mA
- `wTotalLength` rebuilt from the parameter table: `0x008d`

| iface | endpoints | IN | OUT | bInterval | report descriptor |
|---|---|---|---|---|---|
| 0 | 1 | 8 | — | 1 | `+0x8` (68 B) |
| 1 | 2 | 64 | 64 | 1 | `+0x4c` (34 B) |
| 2 | 1 | 21 | — | 1 | `+0x6e` (182 B) |
| 3 | 1 | 19 | — | 4 | `+0x124` (23 B) |
| 4 | 1 | — | 64 | — | `+0x13b` (327 B) |

## Routing

| endpoint | iface | dir | bytes | purpose | context | confidence | required |
|---|---|---|---|---|---|---|---|
| `0x81` | 0 | IN | 8 | boot keyboard, 8-byte report | IRQ6 (USB device) -> usbd_irq_Queue -> USB task; report produced by the scan path | strongly-inferred | **yes** |
| `0x85` | 1 | IN | 64 | vendor page 0xFF00 response, 64-byte frame | VendorHID_CommandDispatcher@0x18001fbe -> VendorHID_SendResponse64@0x18000a70 -> FUN_18018bd6(iface=1) | observed | no |
| `0x0d` | 1 | OUT | 64 | vendor page 0xFF00 command, 64-byte frame | USB controller -> FUN_18016104 (ops table 0x18016d44) -> FUN_18000aec -> buffer 0x180233a8 -> dispatcher 0x18001fbe | observed | no |
| `0x8c` | 2 | IN | 21 | consumer/system controls plus a vendor event collection | application task context; producer not traced in 5B | strongly-inferred | no |
| `0x8e` | 3 | IN | 19 | NKRO keyboard bitmap, 19-byte report | same producer as interface 0's report; not separated in 5B | strongly-inferred | no |
| `0x0f` | 4 | OUT | 64 | HID usage page 0x59 LampArray, 64-byte OUT plus feature reports | USB controller -> class ops; lighting consumer not traced in 5B | strongly-inferred | no |
| `0x00` | -1 | control | 64 | control transfers, including GET_DESCRIPTOR | IRQ6 (USB device) -> usbd_ep0_Queue -> USB task -> FUN_18018082 builds the configuration descriptor | observed | **yes** |

## Checks

- PASS — idVendor matches the host device descriptor (0x0b05)
- PASS — idProduct matches the host device descriptor (0x1b7e)
- PASS — bcdDevice matches the host device descriptor (0x0159)
- PASS — bNumInterfaces matches the host configuration descriptor (5)
- PASS — bMaxPower matches the host configuration descriptor (250 units = 500 mA)
- PASS — the stored attribute bits are the host's minus the mandatory 0x80 (stored 0x20 vs host 0xa0)
- PASS — wTotalLength recomputed from the table equals the host's (built 141 (0x8d) vs host 141)
- PASS — interface 0 endpoint count matches the host (1 vs 1)
- PASS — interface 1 endpoint count matches the host (2 vs 2)
- PASS — interface 2 endpoint count matches the host (1 vs 1)
- PASS — interface 3 endpoint count matches the host (1 vs 1)
- PASS — interface 4 endpoint count matches the host (1 vs 1)
- PASS — the table's interval byte matches every host IN endpoint's bInterval (1, 1, 1, 4)
- PASS — the OUT endpoints' bInterval is NOT in the table — recorded unresolved, not inferred (host OUT bInterval is 4 for both 0x0d and 0x0f; no record holds it)
- PASS — every host endpoint's wMaxPacketSize appears in the table (8, 64, 64, 21, 19, 64)
- PASS — interface 0's report descriptor is byte-identical to the host's (68 bytes at region+0x8)
- PASS — interface 1's report descriptor is byte-identical to the host's (34 bytes at region+0x4c)
- PASS — interface 2's report descriptor is byte-identical to the host's (182 bytes at region+0x6e)
- PASS — interface 3's report descriptor is byte-identical to the host's (23 bytes at region+0x124)
- PASS — interface 4 has NO raw host bytes in this repository — byte-identity is not checkable (interface 4 was unbound on the host; log 09 captured hidraw0-3 only and log 15 preserves lsusb's parsed items, not bytes)
- PASS — interface 4's descriptor is well-formed and the item walk consumes exactly its declared length (155 items over 327 bytes at region+0x13b)
- PASS — interface 4's PARSED item sequence matches the host's parsed listing (item comparison, not byte comparison) (155 firmware items vs 155 host items)
- PASS — the report descriptors are contiguous from +0x8 (+0x8, +0x4c, +0x6e, +0x124, +0x13b)
- PASS — no assembled standard descriptor is stored in the region (device/config/interface/endpoint descriptors are built, not stored)
- PASS — both releases describe the same interface shape (5 interfaces both sides)
- PASS — only bcdDevice differs between the releases (installed 0x0159 vs vendor 0x0158)
- PASS — every route names an endpoint the table accounts for
