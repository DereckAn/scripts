# The polling-rate reader

**Generated by `tool/map_polling_rate_reader.py`. Do not edit by hand.**

> Offline analysis of preserved images and pre-existing read-only Ghidra exports. No device was accessed, no frame was constructed, and nothing was patched. This authorises nothing live.

## Verdict

**The reader is FUN_18004a7e, the actuation compare, which scales per-key timeouts by ten times the multiplier; the PERIOD is set elsewhere, by the entry image's tick job bypassing its own divide-by-eight when the index is 3.**

## Why log 126 could not find it

0x1801e736 is key_state+2, and every reader uses an immediate offset, so no literal equal to the address exists. The reader was already resolved in the Ghidra peripheral census, which log 126 did not consult.

## Every access to the multiplier byte

| dir | instruction | function | base | off | source |
|---|---|---|---|---|---|
| read | `0x180053c4` | `FUN_18004a7e` | `r0@0x1801e734` | 2 | ghidra peripheral census |
| read | `0x180054ac` | `FUN_18004a7e` | `r12@0x1801e734` | 2 | ghidra peripheral census |
| read | `0x18005534` | `FUN_18004a7e` | `r0@0x1801e734` | 2 | ghidra peripheral census |
| read | `0x180055a2` | `FUN_18004a7e` | `r12@0x1801e734` | 2 | ghidra peripheral census |
| write | `0x1800117e` | `FUN_18000d56` | `r1@0x1801e736` | 0 | ghidra peripheral census |
| write | `0x18001548` | `FUN_18000d56` | `r2@0x1801e736` | 0 | ghidra peripheral census |
| write | `0x180018f8` | `FUN_18000d56` | `r1@0x1801e736` | 0 | ghidra peripheral census |
| write | `0x18001ece` | `FUN_18000d56` | `r1@0x1801e736` | 0 | ghidra peripheral census |
| write | `0x18002b50` | `FUN_18001fbe` | `r1@0x1801e736` | 0 | ghidra peripheral census |
| write | `0x18007a1e` | `NO FUNCTION BODY` | `literal@0x1801e736` | 0 | log 126's aligned-word literal search — the census iterates  |

## What the reader does with it

All four sites run the identical arithmetic:

```
180053c0  ldrb.w r2,[lr,#0x22]        ; a per-key byte parameter
180053c4  ldrb   r0,[r0,#0x2]         ; THE MULTIPLIER, 1 << index
180053ca  add.w  r0,r0,r0,lsl #2      ; x5
180053ce  lsls   r0,r0,#0x1           ; x2   -> x10
180053d0  muls   r2,r0,r2             ; parameter x 10 x multiplier
180053d2  ldr.w  r0,[r12,r4,lsl #2]   ; a per-key 32-bit counter
180053d6  cmp    r2,r0                ; has the counter reached it?
```

and the counter is a tick counter, incremented once per invocation:

```
18005488  ldr.w  r3,[r8,r4,lsl #2]
1800548c  adds   r3,r3,#0x1
1800548e  str.w  r3,[r8,r4,lsl #2]
```

The parameter comes from `0x180202d8 + layer*0xd84 + key*0x20 + 0x22`. 0x361*4 = 0xd84 is log 125's keymap-bank layer stride and 0x20 is its per-key record size, so this is the keymap bank's per-key record array. The +0x20/+0x22 pair lands at offset 0 and 2 of the record one stride further on, which is consistent with log 125's record 0 being the bank header — recorded as the reading it is, not asserted as a field name. Log 125 named +0x04..+0x1c; +0x20/+0x22 are outside what it named and this step does not name them either.

## Where the period is actually set

Not here. `0x4ba` in the **entry image** — the tick job logs 109, 110 and 119 traced — reads the profile block at 0x18021de0 + 0x4f8, masked with 0xf, and branches on it:

```
     4d4: ldr    r0,[pc,#0xe0]   ; -> 0x18021de0, THE PROFILE BLOCK
     4d6: ldrb.w r0,[r0,#0x4f8]  ; the polling-rate field
     4da: and    r1,r0,#0xf      ; the index
     4de: ldr    r0,[pc,#0xdc]   ; -> 0x1801e6a4, the /8 counter
     4e0: cmp    r1,#0x3         ; <-- THE BYPASS TEST
     4e2: beq    0x4fc           ;     index 3 -> run EVERY tick
     4e4: ldrb   r1,[r0]
     4e6: adds   r1,r1,#0x1
     4ea: strb   r1,[r0]
     4ec: cmp    r1,#0x8         ; <-- the divide-by-eight
     4ee: beq    0x4fc
     4f0: bl     0x4044          ;     otherwise: sample only
```

A TWO-WAY branch, not a ladder and not a table: index 3 bypasses the divider, every other value takes it.

| index | divisor |
|---|---|
| 0 | 8 |
| 1 | 8 |
| 2 | 8 |
| 3 | 1 |

Gated on the rate:

- `0x4058` → `0x180045b6` — FUN_180045b6 — the state/effect helper
- `0x4044` → `0x180049a4` — the sample fetch (log 119)
- `0x4062` → `0x18004a7e` — FUN_18004a7e — THE ACTUATION COMPARE
- `0x406c` → `0x180057fe` — FUN_180057fe
- `0x4076` → `0x180061c2` — FUN_180061c2 — the report builder

**Not** gated — these run on every tick at either rate:

- `0x4044` → `0x180049a4` — the sample fetch — ALSO on the else path, so Hall sampling is never gated by the rate
- `0x404e` → `0x1800417e` — FUN_1800417e — the 21-byte EP 0x8c sender

The tick job is **byte-identical in both firmware releases**, and the vendor image carries the same four reads at `0x1801e70a`.

## The period, and real units on IRQ38

The informative state is the **slow** one. At index 3 the host polls EP `0x81` every 125 us anyway, so a 125 us grid there is equally explained by the host's schedule. At index 0 the measured grid is **1 ms**, which the host's polling cannot produce — so the device is making data available only every millisecond. The gate makes index 0 run the report stage every eighth tick.

    8 x T = 1 ms   ->   **T = 125.0 us, IRQ38 = 8000 Hz**

The fast state is then a **prediction**: divisor 1 gives 0.125 ms, and log 126 measured 0.125 ms.

Confidence: **strongly-inferred** — the gate and the divider are read out of the instruction stream (observed); the 1 ms device grid is measured on the wire (observed); combining them additionally assumes the report stage makes at most one new report available per invocation, which is not separately proven. That assumption is what keeps this strongly-inferred rather than observed.

Would be refuted by: a report stage that coalesced or emitted more than one report per invocation, or a second ungated producer for EP 0x81.

log 124 recorded 8000/8 = 1000 as 'a consistency, not a measurement', written down so a future step could test it. It holds.

**Not resolved:** the clock CONFIGURATION. No crystal value, PLL multiplier or divider register is recovered by this step, and the dependency map's clock_frequency service stays unresolved.

## The parameter's unit

| index | divisor | multiplier | job period | invocations per unit | real time per unit |
|---|---|---|---|---|---|
| 0 | 8 | 1 | 1.0 ms | 10 | 10.0 ms |
| 3 | 1 | 8 | 0.125 ms | 80 | 10.0 ms |

the multiplier is 1 << index and the divisor is 8 for every index but 3, where it is 1. multiplier x divisor is 8 in both reachable states, so parameter x multiplier x 10 job invocations is the same real duration at either rate. THIS IS THE CORROBORATION THAT MATTERS: two unrelated code sites, in two different images, agree on one rate model, and neither was used to derive the other.

## Would 2000 and 4000 Hz work?

**NO.** the period is not set by the multiplier. It is set by the entry image's tick job, whose test is `cmp r1,#3` — a two-way branch. Index 1 or 2 fails it and takes the divide-by-eight path, so the report stage would still run at the 1000 Hz cadence.

The multiplier WOULD become 2 or 4, so FUN_18004a7e would scale every per-key timeout by 20 or 40 job invocations per unit while the job period stayed at the 1000 Hz value. Hold and tap timings would come out 2x and 4x too long.

What would actually be required:

- the dispatcher's two compares at 0x18002b30 and 0x18002b34, so the device accepts the index at all
- AND the divider selection in the ENTRY image's tick job at 0x4e0/0x4ec, replaced by a rate-dependent divisor

divisor 8 >> index pairs exactly with the existing multiplier 1 << index: their product is 8 for all four indices, so the parameter stays in the same unit at 1000, 2000, 4000 and 8000 Hz. ASUS's SCALING IS ALREADY FOUR-RATE CAPABLE; only the gate is hardcoded to the two endpoints.

EP 0x81's bInterval is already 1 — 125 us at high speed — so the host polls fast enough for any of the four rates without re-enumeration (log 126).

**Not verifiable offline:** CPU headroom. At 2000 or 4000 Hz the gated stage would run two or four times more often than at 1000, and nothing offline measures whether it fits.

Nothing was patched. This is analysis.

## What a custom firmware must reproduce

- sample on EVERY tick. Veneer 0x4044 is called on BOTH paths of the gate, so Hall acquisition is never slowed by the polling rate — only the decision and report stage is.
- gate the actuation compare and the report builder together. Veneers 0x4058, 0x4062, 0x406c and 0x4076 are one block behind one test; splitting them changes which stage sees which sample.
- leave the 21-byte EP 0x8c sender ungated. Veneer 0x404e is a tail call on every tick regardless of the rate.
- scale every tick-denominated timeout by the same factor, or hold and tap timings move when the user changes the polling rate. The stock firmware does this with one cached byte and a x10.
- keep bInterval at 1. The host must poll faster than the fastest rate offered, and the descriptor never changes (log 126).

## The search, step by step

| searched | found | boundary |
|---|---|---|
| the Ghidra peripheral census, all six exports | 4 reads and 5 writes of 0x1801e736; every read in FUN_18004a7e, base 0x1801e734 offset 2. THIS IS THE ANSWER, and it was in the tree before log 126 ran. | the census iterates FUNCTIONS, so code Ghidra never assigned to a function is invisible to it — which is why it shows five writes and not six. |
| aligned-word literals equal to 0x1801e736 | 6 slots, 6 load sites, all six followed by a store (log 126). | a reader using [base+2] leaves no such literal, so this method could never have found the reader. |
| aligned-word literals within +/-0x100 of 0x1801e734, every image | 9 slots in the installed application; 0 in the entry image; 0 in the second-context RAM image. | this is the method that closes the middle case, and its emptiness in the second context is what rules that context out. |
| every pc-relative load of those 9 slots | 44 sites in 17 functions, plus 4 in code with no function body. | the 4 function-less sites were read by hand: they reach +0x50, +0x6, +0x54, +0x60, +0x30 and +0x2c, and one writes 0x1801e78a — the same false-positive shape log 126 rejected, now positively attributed to FUN_180045b6. |
| movw/movt construction of 0x1801e736, every image | none (log 126). | the third and last way to reach the address. |
| the vendor release, independently | the same 4 reads at 0x1801e70a in FUN_18004a7e and the same 5 writes; the entry image's tick job is BYTE-IDENTICAL between releases. | a coincidence would not survive the relocation. |
| the second execution context | no literal near the key-state struct, and no reference to the profile block anywhere in its image. | log 122's parameter-register blind spot does not apply here: both consumers were found through bases the census had already resolved. |

## What this does and does not change

| prior result | status |
|---|---|
| log 124 | its residual negative — 'no consumer of a rate index in the preserved images' — is now ANSWERED, and its 8000/8 lead is confirmed as a measurement. Log 124 is not rewritten: it was correct for its evidence, and its own tool remains its sealed record. |
| log 126 | its 'six writers, no reader' stands as a statement about the searches it ran. The reader existed and the byte shape was the reason it was invisible. |
| logs 110 and 119 | 'the actuation comparison runs on IRQ38's tick divided by 8' is refined: divided by 8 at 1000 Hz, and EVERY tick at 8000 Hz. Both logs described the else path correctly, which is the path the device takes at its default rate. |

## Checks

| | check | detail |
|---|---|---|
| PASS | the multiplier byte has exactly four readers | 0x180053c4, 0x180054ac, 0x18005534, 0x180055a2 |
| PASS | every reader is FUN_18004a7e, the actuation compare | 4 reads, one function |
| PASS | every reader reaches it as key_state+2, which is why an aligned-word search for the address could not find it | base 0x1801e734 offset 2 |
| PASS | the six writers log 126 found are all accounted for | 0x1800117e, 0x18001548, 0x180018f8, 0x18001ece, 0x18002b50, 0x18007a1e |
| PASS | the census sees five of the six writers, and the sixth is in code with no function body — neither method is sufficient alone | 5 in the census, 1 only from the byte search |
| PASS | the application's instruction at 0x180053c0 is the one this model cites | f89e 2022 == f89e 2022 |
| PASS | the application's instruction at 0x180053c4 is the one this model cites | 7880 == 7880 |
| PASS | the application's instruction at 0x180053ca is the one this model cites | eb00 0080 == eb00 0080 |
| PASS | the application's instruction at 0x180053ce is the one this model cites | 0040 == 0040 |
| PASS | the application's instruction at 0x180053d0 is the one this model cites | 4342 == 4342 |
| PASS | the application's instruction at 0x180053d2 is the one this model cites | f85c 0024 == f85c 0024 |
| PASS | the application's instruction at 0x180053d6 is the one this model cites | 4282 == 4282 |
| PASS | the application's instruction at 0x180054ac is the one this model cites | f89c c002 == f89c c002 |
| PASS | the application's instruction at 0x180054b0 is the one this model cites | eb0c 0c8c == eb0c 0c8c |
| PASS | the application's instruction at 0x180054b4 is the one this model cites | ea4f 0c4c == ea4f 0c4c |
| PASS | the application's instruction at 0x180054b8 is the one this model cites | fb01 f10c == fb01 f10c |
| PASS | the application's instruction at 0x18005488 is the one this model cites | f858 3024 == f858 3024 |
| PASS | the application's instruction at 0x1800548c is the one this model cites | 1c5b == 1c5b |
| PASS | the application's instruction at 0x1800548e is the one this model cites | f848 3024 == f848 3024 |
| PASS | the application's instruction at 0x18005534 is the one this model cites | 7880 == 7880 |
| PASS | the application's instruction at 0x1800553c is the one this model cites | 4343 == 4343 |
| PASS | the application's instruction at 0x180055a2 is the one this model cites | f89c c002 == f89c c002 |
| PASS | the application's instruction at 0x180055ae is the one this model cites | fb00 f00c == fb00 f00c |
| PASS | the entry image's instruction at 0x4cc is the one this model cites | 4839 == 4839 |
| PASS | the entry image's instruction at 0x4d0 is the one this model cites | 2801 == 2801 |
| PASS | the entry image's instruction at 0x4d4 is the one this model cites | 4838 == 4838 |
| PASS | the entry image's instruction at 0x4d6 is the one this model cites | f890 04f8 == f890 04f8 |
| PASS | the entry image's instruction at 0x4da is the one this model cites | f000 010f == f000 010f |
| PASS | the entry image's instruction at 0x4de is the one this model cites | 4837 == 4837 |
| PASS | the entry image's instruction at 0x4e0 is the one this model cites | 2903 == 2903 |
| PASS | the entry image's instruction at 0x4e2 is the one this model cites | d00b == d00b |
| PASS | the entry image's instruction at 0x4e4 is the one this model cites | 7801 == 7801 |
| PASS | the entry image's instruction at 0x4e6 is the one this model cites | 1c49 == 1c49 |
| PASS | the entry image's instruction at 0x4ea is the one this model cites | 7001 == 7001 |
| PASS | the entry image's instruction at 0x4ec is the one this model cites | 2908 == 2908 |
| PASS | the entry image's instruction at 0x4ee is the one this model cites | d005 == d005 |
| PASS | the entry image's instruction at 0x4f0 is the one this model cites | f003 fda8 == f003 fda8 |
| PASS | the entry image's instruction at 0x4fe is the one this model cites | 7001 == 7001 |
| PASS | the tick job's literal is the profile block the 51 31 handler writes | *(0x5b8) = 0x18021de0 |
| PASS | every gated veneer decodes to the target log 119 resolved independently | 0x4058->0x180045b6; 0x4044->0x180049a4; 0x4062->0x18004a7e; 0x406c->0x180057fe; 0x4076->0x180061c2 |
| PASS | the ungated veneers decode to their known targets too | 0x4044->0x180049a4; 0x404e->0x1800417e |
| PASS | the gate is a two-way branch: only index 3 bypasses the divider | {"0": 8, "1": 8, "2": 8, "3": 1} |
| PASS | the tick job is byte-identical in both firmware releases | 1.58 and 1.59 carry the same gate |
| PASS | the scaled parameter lives in log 125's keymap bank — 0x361*4 is its layer stride | stride 0xd84 = 0xd84 |
| PASS | IRQ38's period follows from the slow state's measured 1 ms grid and the divide-by-eight | 125.0 us = 8000 Hz |
| PASS | the fast state is then PREDICTED, not fitted, and the prediction matches the wire | predicted 0.125 ms, measured 0.125 ms |
| PASS | the per-key parameter's real duration is the same at both rates, which is the whole point of the multiplier | 10.0 ms per unit at index 0 and index 3 |
| PASS | patching only the handler's two compares would NOT give 2000 or 4000 Hz | the period is not set by the multiplier. It is set by the entry image's tick job... |
| PASS | nothing was patched — the feasibility answer is analysis only |  |
| PASS | log 126's measurement is read from its model rather than restated | notes/polling-rate-protocol.json |
| PASS | log 126's own numbers still say index 0 is the slow state | index 0 85.1% vs index 3 12.2% on the 1 ms grid |
| PASS | the census this whole step rests on is populated, so its silence elsewhere means something | 10740 ACCESS records |
| PASS | the same filter finds neighbouring struct fields, so it is not keyed to one lucky address | 4 accesses to key_state+9 |

`RESULT polling_rate_reader_ok=True checks=53`
