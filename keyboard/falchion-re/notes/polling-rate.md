# The polling-rate path

**Status: no consumer found; no units attached.** Generated by `tool/map_polling_rate.py`. Do not edit by hand.

> Offline analysis of preserved images. No device was accessed and no frame of any kind was constructed. This authorises nothing live.

## Verdict

**No polling-rate index is consumed anywhere in the preserved images, and no real units are attached.**

## Where the index comes from

The historical profile carries `performance.pollingRate = '3'` — an index, not a rate, and the block contains nothing else. Previously observed on hardware; not reproducible from the pcaps in this repository.

the observed command surface is a version/query opcode, two init handshakes, the key-configuration opcodes and the commit. NO POLLING-RATE COMMAND APPEARS IN IT. The note independently records SetPollingRate and GetPollingRate as host HAL method names, and lists the polling-rate opcode among those that 'have known HAL method names; none captured yet'. So a polling-rate command very probably EXISTS on the wire and was simply never captured — which is why this step had to be answered from code, and why the negative below is about the firmware's consumers rather than about the protocol.

| observed command | meaning |
|---|---|
| `12 <sub>` | GET / query. Reply echoes `12 <sub>` then appends data. |
| `22 01` | init handshake (echo only) |
| `51 21 ...` | **set Fn-layer key binding** |
| `51 22 ...` | set — exact function unconfirmed, seen with `C8` (200) payload |
| `50 55` | **commit to flash.** Reply ~220 ms later. |

## Tables

- index-to-Hz tables found in any of the four images: **0**
- divisor-shaped byte runs examined: **2**

the byte runs that match a divisor pattern are examined individually below and none is a rate table: the application's 1,2,4,8,16 run sits in a data area among pointers and is a bit-position table, and the region's ascending run is an identity sequence. Neither is indexed by anything rate-related.

## The five candidates

| candidate | verdict | basis |
|---|---|---|
| a_timer_register | **unresolved** | the application and entry image touch four peripheral windows: the converter trio and its neighbour, 0x40020000, the block the USB stack drives, and the 15-register system-control block log 123 enumerated. None has a reload/divisor shape, and log 109 step 3 already recorded that the timer raising IRQ38 is unidentified. A rate index cannot be traced to a timer that has not been found. |
| b_prescaler_ladder | **eliminated** | every divisor is encoded in the instruction stream — four `cmp rN,#imm` and one `lsls #0x1f` bit test for the divide-by-two. Not one is loaded from RAM, so no stored index can change the ladder. |
| c_mailbox | **eliminated** | log 121 enumerated the mailbox exhaustively: a fault counter, a channel table, a drive table, raw samples and travel bytes. All are sized for 75 keys and none carries a rate or an interval. |
| d_descriptor_binterval | **eliminated** | the bInterval bytes are fixed in the region image, the RAM copy's bInterval bytes are referenced by no aligned word anywhere in the application, and the descriptor builder writes only the descriptor it emits, never the parameter table. Changing the polling rate this way would also require re-enumeration, and nothing triggers one. |
| (e) more than one | n/a | four are eliminated and the fifth is unconfirmable, so no combination survives |

## The shared tick

6 subsystems ride IRQ38 and its divide-by-8 job:

| client | reached via | evidence |
|---|---|---|
| sample fetch | the /8 job, veneer 0x4044 | log 119 |
| actuation compare | the /8 job, veneer 0x4062 | log 110, log 119 |
| watchdog feed | FUN_00000516, the divide-by-8 job | log 114 |
| mailbox cluster | FUN_00000516, veneer 0x40d0 | log 121 |
| lighting swap and shadow read | FUN_00000516, veneers 0x40f8 and 0x4116 | log 122 |
| report builder | the every-tick job, veneer 0x4076 | log 109 |

*If* the period changed: if a stored rate DID change IRQ38's period, every client above would scale with it — the Hall sample rate, the actuation comparison, the watchdog feed margin, the lighting frame rate and the report cadence are all derived from the same interrupt and the same fixed ladder.

**The answer.** the question is moot on this evidence. No stored value reaches the tick at all: the ladder is immediates, no timer block is identified, the mailbox carries no rate and the descriptor is static. So the images do not show report timing and acquisition being coupled through a configurable rate, because they show no configurable rate.

**What a replacement must preserve.** the coupling itself. Anything that changes IRQ38's period changes all six clients at once, including the watchdog margin log 114 recorded as riding the divide-by-8 job. A replacement that reasons about one client's rate in isolation is reasoning wrongly, and that is true whether or not a rate setting exists.

## Units

- Hz attached to anything by this step: **False**
- IRQ38's period: **UNRESOLVED**
- only unit-bearing timing in the project: log 120's delay, which divides a measured core clock by 1000000 and is therefore in microseconds BY CONSTRUCTION, without the clock's numeric value being known
- dependency map: **none — clock_frequency stays unresolved**

*A lead, deliberately not a finding.* log 107 reads EP 0x81's bInterval=1 as 125us (8000 Hz) and EP 0x8e's bInterval=4 as 1 ms (1000 Hz), and the divide-by-8 job would turn an 8000 Hz IRQ38 into exactly 1000 Hz. That is a CONSISTENCY between two independently recovered numbers, not a measurement, and it is recorded here so a future step can test it rather than inherit it as fact.

## Answers

### Where does the polling-rate index land?

Nowhere traceable in the preserved images. The historical profile carries it as the index "3", the observed wire record contains no polling-rate command at all, and no image holds an index-to-rate table.

- confidence: **observed**
- basis: the profile decode, the protocol note's command table, and a search of all four images for a table containing three or more of the plausible rate values
- evidence: this log step 1, notes/ac-profile3-decoded.json, notes/protocol.md

### Could it change the prescaler ladder?

No. All five ladder decisions are encoded in the instruction stream — four `cmp rN,#imm` and one bit test — and none is loaded from RAM. The ladder is fixed at compile time.

- confidence: **observed**
- basis: the five encodings, decoded from the entry image's bytes
- evidence: this log step 2, log 109 step 4

### Could it change the descriptor's bInterval?

Not at runtime. The bIntervals are fixed in the region image at 1, 1, 1, 4, 1, the RAM copy's bInterval bytes are referenced by no aligned word in the application, and the descriptor builder writes only the descriptor it emits. Changing them would also need a re-enumeration that nothing triggers.

- confidence: **observed**
- basis: the region bytes, an aligned-word search for each RAM bInterval address, and the builder's stores
- evidence: this log step 2, log 107 step 3

### Could it be forwarded to the second context?

No. Log 121 enumerated the mailbox's five fields exhaustively — a fault counter, a channel table, a drive table, raw samples and travel bytes. None is a rate or an interval.

- confidence: **observed**
- basis: log 121's model, read back from its generated JSON
- evidence: this log step 2, log 121

### Could it program a timer?

Not confirmable, because no timer block has been identified in any image. The application and entry image touch four peripheral windows and none has a reload or divisor shape. Log 109 already recorded that the timer raising IRQ38 is unidentified; this step does not change that.

- confidence: **unresolved**
- basis: a window census of both images, with no reload-shaped register in any of them
- evidence: this log step 2, log 109 step 3, log 123

### Does the rate setting change the shared tick?

The question is moot on this evidence, and that is itself the answer worth recording. Six subsystems ride IRQ38 and its divide-by-8 job — sample fetch, actuation compare, watchdog feed, mailbox cluster, lighting and the report builder — so IF the period changed they would all scale together. But no stored value reaches the tick, so the images show no configurable rate to couple anything to.

- confidence: **observed**
- basis: the four eliminations above, against the tick-client list assembled from logs 109, 114, 119, 121 and 122
- evidence: this log step 4

### Are real units now attached to anything?

No. Nothing here converts a tick into Hz. The only unit-bearing timing in the project remains log 120's microsecond delay, which gets its unit by dividing a measured clock by a million rather than by knowing the clock. IRQ38's period stays unresolved and the dependency map is untouched.

- confidence: **observed**
- basis: the absence of any recovered rate value, divisor or clock constant
- evidence: this log step 3, log 120

## Unresolved

- **rate_destination** — Where a polling-rate index goes, if the device accepts one at all. It may be handled by a command never captured, by a settings field whose format log 111 could not recover, or not by the device at all.
- **irq38_timer** — The timer that raises IRQ38 is still unidentified, so its period cannot be read or reasoned about even in principle from these images.
- **settings_format** — Log 111 recorded the settings blob's format as unrecovered. If the rate is persisted, it is persisted somewhere this project cannot yet parse.
- **uncaptured_commands** — The historical wire record is a sample, not a specification. A polling-rate command may exist and simply never have been sent while captures were running.
- **high_speed_assumption** — Reading bInterval=1 as 125 microseconds assumes the device enumerates at high speed. That reading comes from log 107 and is inherited here, not re-derived.
- **suggestive_alignment** — That 8000 divided by 8 equals 1000, matching two independently recovered bInterval readings, is a consistency and not a measurement. It is recorded as a lead for a future step, deliberately not as a finding.

## Checks

- PASS — both preserved source hashes match the allowlist
- PASS — the historical profile's polling-rate index is read from the decode ('3')
- PASS — the performance block contains nothing but that index (no Hz, no divisor, no interval)
- PASS — the observed command table is parsed (5 commands)
- PASS — no polling-rate command appears in the observed command table (so the wire record cannot settle this)
- PASS — the note records the opcode as a known HAL name, never captured (SetPollingRate, GetPollingRate)
- PASS — no index-to-Hz table exists in any of the four images (0 found)
- PASS — divisor-shaped byte runs DO exist and were examined individually (2 candidates, none a rate table)
- PASS — the prescaler ladder is compile-time immediates (8, 5, 10, 10)
- PASS — the region's static bIntervals match log 107 ([1, 1, 1, 4])
- PASS — no aligned word references any RAM bInterval byte (so the descriptor is not a runtime mechanism)
- PASS — the mailbox carries no rate field (5 fields, none a rate)
- PASS — no timer block is identified, so candidate (a) is UNRESOLVED rather than eliminated (5 peripheral windows)
- PASS — the tick's clients are enumerated from prior logs (6 clients)
- PASS — the tick is not reconfigurable from the preserved images
- PASS — no real units are attached to anything by this step
- PASS — IRQ38's period stays unresolved
- PASS — the dependency map's clock entry is untouched (it stays unresolved, as the step required)
- PASS — the 8000/8 alignment is recorded as a consistency, not a finding
- PASS — at least one claim is left unresolved (the timer)
- PASS — every claim carries a known confidence label
- PASS — every claim carries a kind_basis and a citation
- PASS — the unresolved boundaries are enumerated (6 entries)
