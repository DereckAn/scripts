# RGB / LampArray route (Phase 5F)

Generated by `tool/map_rgb_lamparray.py`. Do not edit by hand.

**Classification: implemented.** RGB is IMPLEMENTED in the recovered firmware: all six LampArray reports are handled, lamps are addressed by LampId through a coordinate table, and a 306-byte frame buffer is written with intensity-scaled RGB. What is NOT recovered is the final hardware interface, so a replacement cannot yet drive the LEDs even though it can reproduce the protocol.

## Reports

Sizes are measured from the descriptor's own items. Names are spec-derived from HID usage page `0x59`.

| ID | spec name | payload+ID | kind | firmware |
|---|---|---|---|---|
| 1 | LampArrayAttributesReport | 22+1 | Feature | GET |
| 2 | LampAttributesRequestReport | 2+1 | Feature | SET |
| 3 | LampAttributesResponseReport | 28+1 | Feature | GET |
| 4 | LampMultiUpdateReport | 50+1 | Feature | SET |
| 5 | LampRangeUpdateReport | 9+1 | Feature | SET |
| 6 | LampArrayControlReport | 1+1 | Feature | SET |

## Route

| stage | installed | vendor | match |
|---|---|---|---|
| callback_registrar | `0x18008f4a` | `0x18008f1e` | identical |
| control_handler | `0x180184b6` | `0x1801848a` | identical |
| feature_callback | `0x18008f12` | `0x18008ee6` | identical |
| frame_consumer_a | `0x1800aba4` | `0x1800ab78` | identical |
| frame_consumer_b | `0x1800aab0` | `0x1800aa84` | identical |
| frame_write | `0x1800c132` | `0x1800c106` | identical |
| get_handler | `0x1800ffaa` | `0x1800ff7e` | identical |
| set_handler | `0x18010102` | `0x180100d6` | structural |

## Frame buffer

- `0x1802505e`, **6 x 17 x 3 = 306 bytes**
- channel order **red, green, blue**, 8 bits each
- intensity: `(channel * intensity) >> 8`
- lamp counts available: `[84, 85, 89, 23, 37, 51, 65, 79]`

## Hardware boundary

UNRESOLVED. The frame consumers reach no resolved MMIO: 79 and 91 unresolved-base accesses and zero resolved. SPI, PWM, GPIO and DMA are all still open, and the 0x40022000 bank is NOT named on the strength of a plausible per-channel shape.

## Safe omission

The BUFFER's idle state is provable: it lives at 0x1802505e, inside the zeroinit region Phase 3 recovered, so it powers up all-zero and only FUN_1800c132 ever writes it. A firmware that omits RGB and never calls that function leaves an all-zero frame. Whether all-zero means LEDS OFF is NOT provable here, because the driver that consumes the frame is unidentified and its polarity is unknown. No shared clock, pin or controller initialisation could be identified either, for the same reason.

## Claims

| claim | confidence | verified against |
|---|---|---|
| interface 4 declares no Output report; all six of its reports are Feature reports, so the LampArray runs over CONTROL transfers and endpoint 0x0f carries none of it | observed | bytes |
| both GET_REPORT and SET_REPORT for Feature reports route through one registered callback at *(0x1801ebb8) | observed | decompiler |
| the callback is FUN_18008f12, installed during INIT_TASK | observed | listing |
| the callback dispatches on bRequest: 1 to the GET handler, 9 to the SET handler, 0xff to a debug print | observed | listing |
| the GET handler returns 0x17 = 23 bytes for report ID 1, which is exactly the descriptor's 22 payload bytes plus the ID prefix | observed | listing |
| SET_REPORT is capped at 64 bytes before the callback is reached | observed | decompiler |
| a lamp is addressed by LampId, translated to a (row, column) pair through a 2-byte-per-lamp table | observed | decompiler |
| every LampId in a multi-update is bounds-checked against the active configuration's lamp count before it is applied | observed | decompiler |
| the frame buffer is 6 rows x 17 columns x 3 bytes = 306 bytes | observed | listing |
| the channel order in the frame is red, green, blue at cell offsets 0, 1 and 2, eight bits each | observed | listing |
| intensity scales each channel as (channel * intensity) >> 8 | observed | listing |
| a lamp outside the frame's bounds is dropped silently, with no error path | observed | listing |
| report ID 6 sets a pair of complementary flags and calls two further functions, one of them with the current mode | strongly-inferred | decompiler |
| the final hardware interface is NOT identified | unresolved | xref |
| frame timing, double buffering and any interaction with the keyboard scan tick are NOT recovered | unresolved | xref |

## Checks

- PASS — the descriptor declares six reports, all Feature (ID 1: ['Feature'], ID 2: ['Feature'], ID 3: ['Feature'], ID 4: ['Feature'], ID 5: ['Feature'], ID 6: ['Feature'])
- PASS — report ID 1's wire size is the 23 bytes the GET handler returns (22 payload + 1 = 23)
- PASS — report ID 4 carries eight lamps (eight 16-bit LampIds and 32 = 8 x 4 channel bytes)
- PASS — every report the descriptor declares is handled by the firmware (1:GET, 2:SET, 3:GET, 4:SET, 5:SET, 6:SET)
- PASS — the frame is 6 x 17 x 3 = 306 bytes
- PASS — every lamp count fits the frame (counts [84, 85, 89, 23, 37, 51, 65, 79] against 102 cells)
- PASS — the hardware boundary is NOT identified
- PASS — the buffer idle state is provable but the hardware's is not
- PASS — every claim carries a confidence from the closed set
- PASS — every claim says what it was verified against
- PASS — the load-bearing frame claims are listing-verified
- PASS — spec-derived names are separated from measured sizes (names come from the page 0x59 specification; every size in this model is measured from the descriptor)
- PASS — every route function has a measured vendor counterpart (counterparts are looked up in Phase 3's match table, never computed)
- PASS — the arithmetic model reproduces the recovered scaling ((255*255)>>8 = 254, (255*128)>>8 = 127)
