# The mailbox conversation and the per-key sample flow

**Status: traced.** Generated by `tool/map_sample_flow.py`. Do not edit by hand.

> Offline analysis of preserved images. No device was accessed. Path-B evidence-gate work: it changes no decision and authorises nothing live.

## The Hall acquisition gate is CLOSED

Previous state: *narrowed, not closed (log 118)*. Mechanism: **DIRECT WRITE through an application-supplied pointer, not a response record**.

the producer is in neither analysed image, and the buffer's address is stored in no image because the second context receives it at run time in a mailbox record. Log 110's negative was correct and complete for the images it could see.

| step | what happens | citation |
|---|---|---|
| the hardware conversion | FUN_1803ae58 and FUN_1803af28 iterate 240 times: write a 16-bit word to 0x40018000, strobe 0x4001b000 with 0x7c then 0, read 0x40019000, store to the in-image array 0x1803c828. | log 118 step 2; this log step 4 |
| in-image staging and validation | FUN_1803901c reads a derived in-image array at 0x1803c5b2 and validates each entry by splitting it into two fields and comparing an XOR against a table, substituting a default when it mismatches. | this log step 4 |
| delivery of raw samples into application RAM | FUN_1803901c stores through the application-supplied pointer at 24 unrolled sites: strh.w rX,[pointer + key*2, #0x3f2]. The pointer comes from the saved cell 0x1803c4ac. | this log steps 3 and 4 |
| normalisation and the travel curve | FUN_1803a6c4 computes delta = reference[key] - sample, scales it by a per-key 32-bit factor, shifts right by 21, clamps at 0x4ff, and indexes a 1280-byte monotonic table at 0x1803be86 whose maximum is 200. | this log step 4 |
| delivery of travel bytes into the travel array | FUN_1803a6c4 stores the table's output with strb.w rX,[pointer + key, #0x35c], 75 keys, which is 0x18034850 — log 110's travel array. | this log step 4; log 110 step 4 |
| the application's actuation comparison | FUN_18004a7e reads the travel bytes and applies travel >= 100 with a 1..99 hold band. | log 110 steps 1 and 6 |
| the cadence | the every-tick job's /8 branch calls veneer 0x4044, which reaches the opcode 0x01/0x02/0x0c client, immediately before veneer 0x4062 runs the actuation comparison. Both ride IRQ38 divided by 8. | log 109 step 5; this log step 5 |

## The opcode table

| op | handler | client | image | request | response | call sites |
|---|---|---|---|---|---|---|
| `0x01` | `FUN_1803901c` | `0x19d4` | entry | none | 75 uint16 samples at pointer+0x3f2, then FUN_1803a6c4 writes 75 travel bytes at pointer+0x35c and 75 uint16 at pointer+0x2c6 | FUN_180047f8, FUN_180049a8, FUN_18006914, FUN_1800ee72 |
| `0x02` | `FUN_1803901c` | `0x19d4` | entry | none | as 0x01 | FUN_180047f8, FUN_180049a8, FUN_18006914, FUN_1800ee72 |
| `0x03` | `FUN_180388c8` | — | — | none | none recovered | — |
| `0x07` | `FUN_18038a4c` | `0x1a36` | entry | memcpy(pointer+0x27a, src, len) when len != 0 | none recovered | FUN_1800ef84, FUN_1800f474 |
| `0x08` | `FUN_18038970` | `0x1a88` | entry | none | none recovered | FUN_1800f71a |
| `0x0b` | `FUN_18039a64` | `0x1acc` | entry | fills pointer+0x1e4 with a packed byte, 5*count entries | none recovered | FUN_18001fbe |
| `0x0c` | `FUN_1803901c` | `0x19d4` | entry | none | as 0x01 | FUN_180047f8, FUN_180049a8, FUN_18006914, FUN_1800ee72 |
| `0x0d` | `FUN_1803af90 inline` | `0x18000136` | app | record+4 = *(0x1801ed6c); record+8 = 4, 5 or 6 | memset(pointer+0x3f2, 150, 0xff) — marks all 75 samples invalid | CandidateB_Main |
| `0x0e` | `FUN_1803a968` | `0x1b38` | entry | none | none recovered | FUN_1800f474 |
| `0x0f` | `FUN_18038b1c` | `0x1b7c` | entry | memcpy(pointer+0x72c, src, len) | none recovered | FUN_1800f0e4 |

## The pointer share

`FUN_18000136, called from CandidateB_Main` sends opcode `0x0d` with record field `+4` set to `*(0x1801ed6c)` = `0x180344f4`.

the second context receives the base of the application's own structure, so every offset of it is writable by the second context. Delivery is therefore capable of being a DIRECT WRITE and does not need a response record.

| offset | written by | direction | |
|---|---|---|---|
| `+0x1e4` | client 0x1acc (opcode 0x0b) | app -> second |  |
| `+0x27a` | client 0x1a36 (opcode 0x07) | app -> second |  |
| `+0x2c6` | FUN_1803a6c4 | second -> app | 75 uint16, the clamped raw value |
| `+0x35c` | FUN_1803a6c4 | second -> app | 75 bytes — THE TRAVEL ARRAY at 0x18034850 |
| `+0x3f2` | FUN_1803901c, and memset by opcode 0x0d | second -> app | 75 uint16 raw samples |
| `+0x72c` | client 0x1b7c (opcode 0x0f) | app -> second |  |

## The travel curve

The normalised value is clamped at `0x4ff` and indexes a table at `0x1803be86` of **1280 bytes** — exactly the clamp bound plus one. It is monotonic non-decreasing, ranges 0..200, and its maximum is exactly twice log 110's actuation threshold of 100. Three independent properties agreeing is what makes the identification safe.

Sampled every 64th entry: `[0, 49, 74, 95, 108, 119, 129, 137, 145, 151, 158, 162, 167, 171, 175, 180, 184, 188, 193, 199]`

## Cadence

The every-tick job's /8 branch calls veneer `0x4044`, whose target `0x180049a4` tail-branches to `0x1801be22`, the sample client's veneer. the sample fetch at 0x4044 runs BEFORE the actuation comparison at 0x4062 in the same /8 branch.

**UNRESOLVED — the timer that raises IRQ38 is still not identified (log 109 step 3)**

## Answers

### Does per-key sample data cross from the second context to the application?

YES, in both directions of the pipeline. Raw 16-bit samples cross at pointer+0x3f2 and derived travel bytes cross at pointer+0x35c, which is the travel array log 110 identified.

- confidence: **observed**
- basis: 24 strh.w stores at offset 0x3f2 and 3 strb.w stores at offset 0x35c, all through the pointer the application supplied in a mailbox record
- evidence: this log steps 3 and 4, log 110 step 4

### By which mechanism — response records or direct writes?

DIRECT WRITES. No response record carries sample data. The application hands over the base of its own structure in record field +4 of opcode 0x0d, and the second context writes the structure's fields itself.

- confidence: **observed**
- basis: FUN_18000136's literal pool holds 0x1801ed6c and the record array; the stores use the saved pointer as their base register
- evidence: this log step 3

### At what cadence?

On IRQ38's tick divided by 8, immediately before the actuation comparison in the same job. The absolute rate remains unresolved because the timer that raises IRQ38 is still unidentified.

- confidence: **observed**
- basis: the entry image's /8 branch calls veneer 0x4044, whose target tail-branches to the client veneer; the branch decoder is checked against one Ghidra resolved
- evidence: log 109 step 5, this log step 5

### Does the second context write the per-key bitmap at 0x18023410?

No. The bitmap is written by the application's FUN_180049a8, which is the function that requests the samples. The second context never references it.

- confidence: **observed**
- basis: the bitmap address appears in FUN_180049a8's literal pool and in no aligned word or immediate of the second-context image
- evidence: this log step 4, log 118 step 3

### Is the Hall acquisition gate closable?

YES. Every link from the hardware conversion to the actuation comparison is now demonstrated, and the tool refuses this wording unless all eight backing checks hold.

- confidence: **observed**
- basis: path_complete() is computed from the pointer share, both delivery counts, three properties of the travel curve and the decoded cadence branch
- evidence: this log step 6, log 110, log 109, log 118

### Does this explain the long-standing callerless functions?

Yes, both of them. FUN_18008c16 — unexplained since log 106 and still open in log 109 — calls FUN_180049a8, which is the sample requester. That closes the last of log 106's large orphans.

- confidence: **strongly-inferred**
- basis: Ghidra's inventory records FUN_180049a8's only caller as FUN_18008c16; what calls FUN_18008c16 is still not recovered
- evidence: log 106, log 109 step 5, this log step 2

## Unresolved

- **absolute_rate** — IRQ38's period is still unknown, so the sample rate is a ratio and not a frequency. Unchanged by this step.
- **fun_18008c16_caller** — FUN_18008c16 calls the sample requester, but what calls FUN_18008c16 is still not recovered. The chain is complete from the tick job's veneer; this second route into it is not.
- **opcode_03** — Opcode 0x03 has a handler and no recovered client. Two of the sixteen opcodes are implemented with no caller found.
- **physical_units** — The converter's input and output carry no units. The travel byte's scale is 0..200 with actuation at 100, which is a ratio, not a distance.
- **reference_and_scale_origin** — The per-key reference values and scale factors the normalisation uses live in the second context's RAM. What writes them is not traced here.
- **unresolved_accesses** — 484 of the second context's accesses still have an unresolved base, so every negative remains a 'not resolved'.

## Checks

- PASS — both preserved source hashes match the allowlist
- PASS — the movw decoder reproduces log 113's hand-validated veneer bytes (41f6517c)
- PASS — the branch decoder agrees with a target Ghidra resolved (bl at 0x180049b4 -> 0x1801be22)
- PASS — every entry-image client has a cross-image veneer (6 of 6)
- PASS — client 0x19d4's opcode is present in its own bytes (0x01, 0x02, 0x0c)
- PASS — client 0x1a36's opcode is present in its own bytes (0x07)
- PASS — client 0x1a88's opcode is present in its own bytes (0x08)
- PASS — client 0x1acc's opcode is present in its own bytes (0x0b)
- PASS — client 0x1b38's opcode is present in its own bytes (0x0e)
- PASS — client 0x1b7c's opcode is present in its own bytes (0x0f)
- PASS — client 0x18000136's opcode is present in its own bytes (0x0d)
- PASS — every client veneer has at least one call site (13 sites)
- PASS — the sender's literal pool holds the travel pointer cell (0x1801ed6c)
- PASS — the sender's literal pool holds the mailbox record array
- PASS — shared base + 0x35c is exactly log 110's travel array (0x18034850)
- PASS — the second context stores into the travel array (3 strb.w sites at +0x35c)
- PASS — the second context stores raw samples into application RAM (24 strh.w sites at +0x3f2)
- PASS — the second context never references the per-key bitmap (0x18023410)
- PASS — the travel table's length equals the clamp bound (1280 == 0x4ff + 1)
- PASS — the travel table is monotonic, as a travel curve must be
- PASS — the table's maximum is exactly twice log 110's actuation threshold (200 = 2 x 100)
- PASS — the every-tick /8 job reaches the sample client (0x4044 -> 0x180049a4 -> 0x1801be22)
- PASS — the absolute rate is still reported as unresolved (a ratio is not a frequency)
- PASS — every link of the producer path is demonstrated (7 links)
- PASS — the Hall acquisition gate is reported CLOSED (and only because path_complete() holds)
- PASS — every claim carries a known confidence label
- PASS — every claim carries a kind_basis and a citation
- PASS — the unresolved boundaries are enumerated (6 entries)
