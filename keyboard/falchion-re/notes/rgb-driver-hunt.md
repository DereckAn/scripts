# The RGB driver hunt

**Status: driver not found; boundary tightened.** Generated by `tool/map_rgb_driver_hunt.py`. Do not edit by hand.

> Offline analysis of preserved images. No device was accessed. This authorises nothing live.

## The result in three lines

- The lighting subsystem is an exhaustive closure of **42 functions** that resolves **zero** peripheral accesses.
- **Double buffering** and **frame timing** are recovered — both were recorded as not found in log 112.
- The safe-idle question is still **unanswerable**, so RGB stays `unresolved`.

## Why the unresolved accesses are not a hidden driver

657 unresolved accesses across the closure:

| reason | count |
|---|---:|
| `base_r0_unknown` | 134 |
| `base_r1_unknown` | 116 |
| `stack_relative` | 112 |
| `base_r2_unknown` | 85 |
| `base_r3_unknown` | 60 |
| `register_offset` | 54 |
| `base_r5_unknown` | 28 |
| `base_r4_unknown` | 27 |
| `base_r6_unknown` | 13 |
| `base_r9_unknown` | 12 |
| `base_r8_unknown` | 8 |
| `base_r7_unknown` | 7 |
| `base_r12_unknown` | 1 |

stack-relative accesses are locals and register-offset accesses index arrays whose base IS known. The base_r0..r3 cases are the ARM parameter registers, so their base is the caller's pointer, not a peripheral. None of these is a concealed MMIO base.

## Candidates eliminated

| candidate | eliminated | basis |
|---|---|---|
| second_context | **True** | the second-context image contains no reference to either frame buffer, no reference to any address in the app's lighting RAM region, and no 306 constant in a word or an immediate. Its own mailbox fields are the fault counter, the channel table and the drive table, all sized for 75 keys, none 306 bytes. |
| bank_0x40022000 | **True** | its users cluster at 0x18011886..0x18011bbe; log 112 said it 'would suit a PWM controller' and did not name it. That suspicion is now actively disfavoured for lighting. |
| usb_block_0x40100000 | **True** | the lighting side touches it only through one status read in FUN_18008c16, which is also the /8 job root |
| dma_setup | **True** | it does not appear in the exhaustive closure, confirming log 112's negative with a computed closure rather than a reachability sample |

## Recovered: double buffering

`0x1800aab0` copies **306 bytes** from the live frame `0x1802505e` to a shadow at `0x18024f2c` — exactly one frame below — gated on `0x1801e6b7 & 0x30`, then tail-calls `0x180089a8`.

```
tst r0,#0x30 / beq skip ; memcpy(frame - 0x132, frame, 0x132) ; strb #0 ; b.w FUN_180089a8
```

This supersedes log 112 step 5: 'no second buffer or swap was found'.

## Recovered: frame timing

**IRQ38 divided by 8 — the same job that feeds the watchdog (log 114) and drives the mailbox cluster (log 121).**

| call site | veneer | app function | role |
|---|---|---|---|
| `0x0550` | `0x40d0` | `0x18001fbe` | calls FUN_1800aba4 and FUN_1800c1b4 |
| `0x0576` | `0x4116` | `0x18008c16` | calls FUN_1800aab0, the frame swap |
| `0x0564` | `0x40f8` | `0x1800b370` | calls FUN_18009222 and FUN_1800b0aa, the shadow readers |

**UNRESOLVED — IRQ38's period is still unidentified.** This supersedes log 112 step 5: 'the frame consumers are not reached from Phase 5C's tick chain' and 'frame timing ... NOT RECOVERED'.

## The safe-idle question

**Answerable: False.**

- *The buffer.* all zero, by construction — both buffers live in the zeroinit region and only FUN_1800c132 writes the live frame
- *The hardware.* NOT DETERMINABLE from the preserved images

What was looked for and not found:

- an output-enable or blank line asserted after the first frame
- a global brightness or current-limit register
- a driver reset or latch sequence in any lighting init path
- any MMIO access whatsoever in the 45-function closure

an output-enable, a brightness register and a reset sequence are all MMIO writes, and the closure contains none. There is nothing to inspect, not merely something unrecognised.

**Boundary before:** both frame consumers reach zero resolved MMIO; 79 and 91 unresolved-base accesses (log 112)

**Boundary now:** the lighting subsystem is an exhaustively enumerated closure of 45 functions that resolves ZERO peripheral accesses. Its unresolved accesses are stack locals, indexed array offsets and caller-supplied pointers, not a concealed peripheral base. Four candidate transports are individually eliminated: the second execution context, the 0x40022000 bank, log 111's DMA setup, and the 0x40100000 block. The frame is produced, intensity-scaled, double-buffered and swapped on the divide-by-8 tick — and then the data path leaves the analysed set entirely.

**Classification: unresolved, unchanged.** RGB moves out of unresolved only with a safe-idle PROOF. Tighter ignorance is not proof. A common-anode part behind an inverting stage would still read an all-zero frame as full brightness, and nothing recovered excludes that.

## Answers

### Did the frame consumers resolve to hardware?

No. The lighting subsystem is an exhaustive closure of 45 functions — 42 in the application and 3 library routines in the entry image — and it resolves ZERO peripheral accesses. The hardware interface is not merely unresolved; it is absent from both analysed images.

- confidence: **observed**
- basis: a computed call-graph closure intersected with the census, rather than log 112's per-function reachability sample
- evidence: this log step 1, log 112 step 5

### What are the unresolved accesses, then?

Not a hidden peripheral base. They decompose into stack-relative locals, register-offset indexing into arrays whose base is known, and base-in-an-argument-register cases where the pointer belongs to the caller. Log 112's counts were correct; reading them as 'a driver is hiding here' would not be.

- confidence: **observed**
- basis: the census's own reason field, tallied across the closure
- evidence: this log step 1

### Does the second execution context drive the LEDs?

No. Its image contains no reference to either frame buffer, none to the application's lighting RAM region, and no 306 constant in any word or immediate. Its mailbox fields are all sized for 75 keys.

- confidence: **observed**
- basis: aligned-word and immediate searches over the whole image
- evidence: this log step 2, log 118, log 121

### What about the 0x40022000 bank and the DMA setup?

Both eliminated. The 0x40022000 users form a cluster at 0x18011886..0x18011bbe whose intersection with the lighting closure is EMPTY, and log 111's DMA setup is not in the closure either. The 0x40100000 block is the USB stack's, touched from the lighting side only by one status read.

- confidence: **observed**
- basis: set intersection of each window's user set with the closure
- evidence: this log step 2, log 111, log 112

### Is there double buffering?

Yes, and log 112 recorded that there was not. FUN_1800aab0 copies 306 bytes from the live frame at 0x1802505e to a shadow at 0x18024f2c, gated on flag bits 0x30, then tail-calls FUN_180089a8. Five functions read the shadow.

- confidence: **observed**
- basis: the swap's listing, with the copy length equal to the frame size and the destination exactly one frame below the source
- evidence: this log step 3, log 112 step 5

### What is the frame timing?

IRQ38 divided by 8. All three lighting roots are called from the prescaler's divide-by-8 job through veneers at 0x40d0, 0x40f8 and 0x4116. Log 112 recorded the consumers as NOT reached from the tick chain; the veneer mechanism logs 119 to 121 established is what makes the link visible. The absolute rate stays unresolved.

- confidence: **observed**
- basis: decoded movw/movt veneer targets and the call sites' offsets inside the job's recovered range
- evidence: this log step 3, log 109 step 5, log 121

### Can the safe-idle question be answered?

No. An output-enable line, a brightness register and a driver reset are all MMIO writes, and the closure contains none — so there is nothing to inspect rather than something unrecognised. The BUFFER's idle state remains provably all-zero; what the hardware does with an all-zero frame remains undetermined.

- confidence: **observed**
- basis: the absence is now a property of an exhaustive closure rather than of a sampled one
- evidence: this log step 4, log 112 step 6

### Does RGB move in the dependency map?

No. It stays UNRESOLVED. The boundary is much tighter, but tighter ignorance is not the safe-idle proof the move requires.

- confidence: **observed**
- basis: the step's own precondition: RGB moves only with the proof
- evidence: this log step 4

## Unresolved

- **final_transport** — The frame is prepared, scaled, double-buffered and swapped on the tick, and then leaves the analysed set. SPI, PWM, GPIO bit-bang and DMA all remain open, but none is reachable from the closure.
- **driver_polarity** — Whether an all-zero frame means dark or bright depends on the driver's polarity, which is unidentified. A common-anode part behind an inverting stage would read all-zero as full brightness.
- **shared_initialisation** — Whether any clock, pin or controller the lighting path initialises is SHARED with a mandatory service is still not established, because the initialisation is not in the closure either.
- **absolute_frame_rate** — The cadence is IRQ38/8, a ratio. IRQ38's period is still unknown, so no frame rate in Hz follows.
- **swap_gate** — The swap is gated on bits 4 and 5 of the byte at 0x1801e6b7. What sets those bits is not traced here.
- **unread_rom** — A mask ROM was never searched and cannot be, so a ROM-resident driver is not excluded — only unevidenced.

## Checks

- PASS — both preserved source hashes match the allowlist
- PASS — the lighting closure is computed, not sampled (42 functions)
- PASS — the closure resolves ZERO peripheral accesses (0 MMIO accesses)
- PASS — the same closure DOES resolve RAM accesses (so the census is working, not silent)
- PASS — the closure has unresolved accesses to explain (657)
- PASS — most unresolved accesses are locals, indexing or parameters (112 stack, 54 indexed, 396 parameter)
- PASS — the second-context hypothesis is eliminated (no frame reference, no lighting-region reference, no 306)
- PASS — the 0x40022000 bank is not reachable from the lighting closure (empty intersection)
- PASS — log 111's DMA setup is not in the closure
- PASS — the 0x40100000 block has many users, and they are the USB stack's (27 functions)
- PASS — double buffering is recovered (306 bytes -> 0x18024f2c)
- PASS — the copy length equals the frame size exactly (0x132 = 6*17*3)
- PASS — the shadow sits exactly one frame below the live buffer
- PASS — every lighting root is called from the divide-by-8 job (3 roots)
- PASS — every veneer decodes to the function it is claimed to reach
- PASS — the absolute frame rate is still reported as unresolved (a ratio is not a frequency)
- PASS — the safe-idle question is reported UNANSWERABLE, not answered
- PASS — RGB stays UNRESOLVED in the dependency map (tighter ignorance is not proof)
- PASS — the boundary is stated more precisely than before
- PASS — every claim carries a known confidence label
- PASS — every claim carries a kind_basis and a citation
- PASS — the unresolved boundaries are enumerated (6 entries)
