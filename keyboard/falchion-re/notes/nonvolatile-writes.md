# Nonvolatile write path (Phase 5E)

Generated by `tool/map_nonvolatile_writes.py`. Do not edit by hand.

**Static tracing only. No command was constructed and no device was accessed.**

## Dispatch answer

**(b)** the commit QUEUES work. The 0x50/0x55 branch contains no call instruction; it sets a command byte that a state machine in another context drains, through two further hops.

## The chain

| step | address | confidence | verified against |
|---|---|---|---|
| vendor OUT mailbox | `0x180233a8` | observed | listing |
| VendorHID_CommandDispatcher | `0x18001fbe` | observed | listing |
| the 0x50/0x55 commit branch | `0x18002428` | observed | listing |
| commit command byte | `0x18022ce4` | observed | listing |
| FUN_18000d56 | `0x18000d56` | observed | decompiler |
| FUN_1800e2a8 | `0x1800e2a8` | observed | listing |
| FUN_1800e2c4 | `0x1800e2c4` | observed | listing |
| FUN_1800e272 | `0x1800e272` | observed | listing |
| storage request struct | `0x18025ef4` | observed | listing |
| FUN_1800dc92 | `0x1800dc92` | observed | xref |
| FUN_18011dd0 | `0x18011dd0` | observed | listing |
| the storage medium | — | unresolved | xref |

## Ranges the path may modify

| range | label | confidence | overlaps app region |
|---|---|---|---|
| `0x320000..0x330000` | the 64 KiB target of the 0xd8 erase | observed | False |
| `0x330000..0x338000` | the 32 KiB target of one 0x52 erase | observed | False |
| `0x340000..0x348000` | the 32 KiB target of another 0x52 erase | observed | False |
| `0x320000..0x720000` | a computed slot: base 0x320000 plus a byte-indexed 0x4000 stride | strongly-inferred | False |

## Settings format

NOT RECOVERED. The commit path passes ADDRESSES to erase primitives; no header, magic, version, length or checksum is constructed or verified anywhere in the traced chain. What the commit persists FROM was not established either: no copy from the 0x180202d8 per-key bank or the 0x18024f0c global table into a staging buffer appears in the traced path.

## Storage medium

UNIDENTIFIED. The opcodes 0xd8 and 0x52 match the JEDEC SPI-NOR block-erase opcodes, which is recognition and not proof. No SPI controller register was identified, the whole storage cluster touches no MMIO, and the only hardware contact is a DMA setup into two unnamed windows. Internal MCU storage, external U5 SPI NOR and a RAM mirror are all still consistent with the evidence.

## Exit gate: omit-all-writes

The evidence supports proving a prototype CANNOT corrupt existing configuration, not implementing safe persistence. The format's magic, version, length and checksum were not recovered, and the storage medium is unidentified, so safe persistence cannot be implemented from this evidence. What CAN be shown is the negative, and it is a short argument: every route into the erase primitives runs through the command byte at 0x18022ce4, and a firmware that never writes that byte and never fills the request struct at 0x18025ef4 cannot reach FUN_1800e2a8 or FUN_1800e2c4.

A custom firmware must never:

- write 0x18022ce4
- write the request struct at 0x18025ef4
- call FUN_1800e2a8 or FUN_1800e2c4, or their vendor counterparts
- dispatch vendor opcode 0x50 subcommand 0x55

## Checks

- PASS — every hop joins two declared steps (12 steps, 11 hops)
- PASS — every step and hop carries a confidence from the closed set
- PASS — every step says what it was verified against
- PASS — the load-bearing steps are listing-verified
- PASS — the commit is recorded as queueing, not as programming storage
- PASS — the storage medium is NOT identified
- PASS — no settings-format field is claimed
- PASS — the exit gate is the omit-all-writes branch
- PASS — no modifiable range overlaps the bootloader's application region (the application region is 0x10000..0x7c000; the targets start at 0x320000)
- PASS — both erase opcodes are recorded with a recognition caveat
- PASS — every FUNCTION step has a measured vendor counterpart (function counterparts are looked up in Phase 3's match table, never computed from a relocation rule)
- PASS — data steps take the flat region shift and code steps claim nothing (a raw address inside a function body has no match row, so none is invented for it)
- PASS — this module constructs no command bytes for transmission (the module exposes no encoder and no transmit helper)
