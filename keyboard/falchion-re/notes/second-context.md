# The second execution context: the 0x18038000 image

**Status: analysed.** Generated by `tool/map_second_context.py`. Do not edit by hand.

> Offline analysis of a preserved image. No device was accessed. This is Path-B evidence-gate work: it changes no decision and authorises nothing live.

## What it is

### Is this a second core's payload, and what does the reset path start?

A service payload, not a full application. The reset handler reads VTOR, loads SP from it, calls one init function and jumps to a three-call main that ends in an endless command loop. There is no scheduler, no task table and no USB or storage code. It is a command-driven server for one client.

- confidence: **observed**
- basis: listing: SC_Vector_Reset at 0x180381c0 reads 0xe000ed08, then FUN_180381a0 calls FUN_1803af90, which never returns
- evidence: log 113 step 4, this log step 1, this log step 4

### Does it produce or transport per-key analog readings?

It TRANSPORTS and CONVERTS them, and it does not deliver them to the application. Opcode 0x0f de-interleaves five groups of 16-bit values out of application RAM at pointer+0x72c, then runs a 240-iteration converter loop whose results land in an in-image array. No recovered path writes those results back.

- confidence: **observed**
- basis: listing: FUN_18038b1c's five-group de-interleave and FUN_1803ae58's strobe/write/readback loop; the census resolves no write into 0x18000000..0x18037fff except one memset
- evidence: this log step 4, log 110 step 4

### Does anything in it write the travel array or the key bitmap?

No. The one demonstrated write into application RAM is a 150-byte memset to 0xff at pointer+0x3f2, on the init opcode. The travel array is at pointer+0x35c and is 150 bytes, so the memset covers the array IMMEDIATELY AFTER it and not the array itself. The key bitmap is never referenced.

- confidence: **observed**
- basis: listing: FUN_18038308 is a byte-replicating memset and is called with (pointer+0x3f2, 0x96, 0xff); aligned-word search finds the bitmap in no word of this image
- evidence: log 110 step 4, log 109, this log step 3

### What does it own that the application does not?

One block outright and two registers. 0x40040000 is touched 98 times from a single init function here and NEVER by the application or the entry image. 0x40018000 and 0x4001c000 are likewise absent from both. Everything else it touches is shared, including the 0x45000000 control block and both watchdog blocks.

- confidence: **observed**
- basis: census set difference against the application's and entry image's own FalchionPeripheralMap output
- evidence: this log step 2, log 100

### Where does its data flow terminate?

In its own RAM. The 240 conversion results are stored to an in-image array and read by one further in-image function. The only outward channels recovered are the mailbox tail index and the single memset into application RAM.

- confidence: **strongly-inferred**
- basis: aligned-word search: the result array is referenced from exactly three literal-pool slots, all inside this image
- evidence: this log step 4

### Does this settle the dual-core question?

It settles the mechanism and not the silicon. There is now a real core-to-core-shaped channel: a ring buffer with a head owned by one image, a tail owned by another, and a start register. Whether the two run concurrently on two cores or in sequence on one is still not shown, because nothing recovered proves the application keeps executing while this image runs.

- confidence: **strongly-inferred**
- basis: listing on both sides: the entry image's client at 0x1b7c and this image's server at 0x1803af90 implement complementary halves
- evidence: log 113 step 4, notes/dual-core-question.md, this log step 4

## The image is the same in both releases

Across `0x74000..0x7bfff` the two releases differ in **4 bytes**, and every one of them lies in the application region's word-sum field at `0x7bffc`. The code is identical. The image's real extent is `0x74000..0x7bffb`; the last word of the range is a container field that happens to fall inside it.

## Vector table

73 entries, 57 external slots. Initial SP `0x1803e458`, reset `0x180381c1`. 56 external slots share the default handler at `0x180381f7`.

| slot | handler | note |
|---|---|---|
| initial_SP | `0x1803e458` | in-image |
| Reset | `0x180381c1` | in-image |
| NMI | `0x180381e9` | in-image |
| HardFault | `0x18038be1` | in-image |
| MemManage | `0x1803970d` | in-image |
| BusFault | `0x180381eb` | in-image |
| UsageFault | `0x180381ed` | in-image |
| Reserved7 | `0x00000000` |  |
| Reserved8 | `0x00000000` |  |
| Reserved9 | `0x00000000` |  |
| Reserved10 | `0x00000000` |  |
| SVCall | `0x180381ef` | in-image |
| DebugMonitor | `0x180381f1` | in-image |
| Reserved13 | `0x00000000` |  |
| PendSV | `0x180381f3` | in-image |
| SysTick | `0x18039b7d` | in-image |
| IRQ3 | `0x180381d7` | in-image |

Exactly one external interrupt has its own handler: **IRQ3**.

## The mailbox protocol

The shared word log 113 found is one field of a ring buffer, and both halves are now recovered.

- `0x20000000` — head index, advanced by the client
- `0x20000004` — tail index, advanced by this image
- `0x20000008..0x20000167` — 8 records of `0x2c` bytes
- record byte 0 is the opcode; the word at +4 carries a pointer into application RAM, saved at the init opcode
- `0x12345678` is written to `0x20000000` once, by the instruction at `0x1803af96`, as a server-is-up signal before the same word becomes the head index

### The command set (10 of 16 implemented)

| opcode | handler | |
|---|---|---|
| `0x00` | `0x1803b1b0` | falls through |
| `0x01` | `0x1803b144` | implemented |
| `0x02` | `0x1803b12c` | implemented |
| `0x03` | `0x1803b156` | implemented |
| `0x04` | `0x1803b1b0` | falls through |
| `0x05` | `0x1803b1b0` | falls through |
| `0x06` | `0x1803b1b0` | falls through |
| `0x07` | `0x1803b168` | implemented |
| `0x08` | `0x1803b17e` | implemented |
| `0x09` | `0x1803b1b0` | falls through |
| `0x0a` | `0x1803b1b0` | falls through |
| `0x0b` | `0x1803b194` | implemented |
| `0x0c` | `0x1803b1b2` | implemented |
| `0x0d` | `0x1803afd4` | implemented |
| `0x0e` | `0x1803b1ac` | implemented |
| `0x0f` | `0x1803b1aa` | implemented |

## MMIO census

353 resolved accesses, **484 unresolved**, across 69 functions. Every negative below is bounded by that unresolved count.

| window | accesses | r/w | application | |
|---|---:|---|---:|---|
| `0x00001000` | 1 | 1/0 | 0 | **own** |
| `0x0800ff00` | 2 | 2/0 | 1 | shared |
| `0x10000000` | 1 | 1/0 | 1 | shared |
| `0x20000000` | 37 | 24/13 | 21 | shared |
| `0x20000f00` | 1 | 1/0 | 2 | shared |
| `0x40008000` | 4 | 0/4 | 4 | shared |
| `0x40009000` | 4 | 0/4 | 4 | shared |
| `0x40018000` | 12 | 3/9 | 0 | **own** |
| `0x40019000` | 10 | 7/3 | 7 | shared |
| `0x4001b000` | 54 | 3/51 | 14 | shared |
| `0x4001c000` | 18 | 9/9 | 0 | **own** |
| `0x40022000` | 11 | 6/5 | 41 | shared |
| `0x4002f000` | 3 | 0/3 | 3 | shared |
| `0x40040000` | 98 | 53/45 | 0 | **own** |
| `0x45000000` | 70 | 49/21 | 78 | shared |
| `0x45000100` | 5 | 1/4 | 7 | shared |
| `0x45000300` | 2 | 1/1 | 2 | shared |
| `0x60000800` | 4 | 2/2 | 2 | shared |
| `0x60011000` | 1 | 1/0 | 0 | **own** |
| `0xe000e400` | 2 | 0/2 | 1 | shared |
| `0xe000ed00` | 13 | 11/2 | 32 | shared |

Windows this image touches and the application and entry image never do: `0x00001000`, `0x40018000`, `0x4001c000`, `0x40040000`, `0x60011000`.

## The shape questions, answered

**An ADC-shaped window?** a write-strobe-readback trio driven by an index: strobe `0x4001b000`, data-out `0x40018000`, data-in `0x40019000`, iterated 240 times. the index varies the DATA written, not the register address, so this is a muxed converter interface and NOT a per-channel register bank.

**The `0x40022000` per-channel bank?** No. 1 register touched. only the base word is touched, by a single bit-15 clear and matching reads; no stride, no channel select.

**DMA descriptors?** no descriptor-shaped structure and no window with the source/destination/count triple a DMA engine needs was resolved; 484 unresolved accesses bound this negative.

**Flash or SPI controller?** two registers in the flash window, read and then written with 1, from the fault handler and one other function; no erase or program sequence was recovered.

**Watchdogs.** log 114 recorded block 0x40009000 as touched exactly once in the whole firmware. That was true of the two analysed images; this image disables BOTH blocks on its own init path, so the scope of that sentence was the analysed set and not the firmware.

## Cross-context references

| value | | found in |
|---|---|---|
| `0x18034850` | travel_array | **nowhere** |
| `0x180344f4` | travel_buffer_base | region1 +0x9ec |
| `0x1801ed6c` | travel_pointer_cell | app_installed +0x3dc +0x4da8 +0x5f48, entry_installed +0x1bd0 |
| `0x18023410` | key_state_bitmap | app_installed +0x4db8 +0x5f08 +0x6368 |
| `0x20000000` | mailbox_base | app_installed +0x154b8, app_vendor +0x1548c |
| `0x20000008` | mailbox_records | app_installed +0x3d8 +0x8f54, app_vendor +0x3d8 +0x8f28, entry_installed +0x1bcc, entry_vendor +0x1bcc, second_context +0x321c |
| `0x12345678` | handshake_token | app_installed +0x3f4, app_vendor +0x3f4, second_context +0x3214 |
| `0x18038000` | second_context_base | entry_installed +0x2004, entry_vendor +0x2004 |
| `0x60074000` | second_context_flash_src | app_installed +0x3f0, app_vendor +0x3f0, entry_installed +0x2000, entry_vendor +0x2000 |
| `0x180381c1` | second_context_reset | second_context +0x4 |

## The Hall-acquisition gate

**NARROWED, NOT CLOSED.**

*What moved.* a hardware acquisition loop IS now recovered, in this image: 240 iterations of write-a-16-bit-word, strobe, read-a-16-bit-word back, over a fixed register trio. Before this step no per-channel converter loop had been found in any image.

*What did not.* that loop does not fill the travel array. Its results terminate in an in-image array, and the travel array's address still appears in no aligned word of ANY image, this one included.

*The boundary now.* the second context reads application RAM at pointer+0x72c and writes application RAM at pointer+0x3f2, which is the 150 bytes IMMEDIATELY AFTER the travel array at pointer+0x35c. The producer of the travel bytes themselves is still unrecovered.

## Unresolved

- **unresolved_accesses** — 484 of this image's accesses have an unresolved base, against 353 resolved. Every negative about what it touches is bounded by that ratio and is stated as 'not resolved', never as 'does not happen'.
- **register_identity** — No block is named. The strobe, data-out and data-in roles are read off an instruction sequence; the peripherals themselves stay unnamed for want of a reference manual.
- **concurrency** — Whether the two contexts execute concurrently is not shown. The client spins waiting for the tail to catch up, which is equally consistent with a second core and with a coroutine on one core.
- **travel_producer** — The producer of the travel bytes at pointer+0x35c is still unrecovered. This image narrows where it is not.
- **converter_source** — The 240 values the loop writes come from an in-image table by default and from application RAM in one mode. What the conversion physically drives and measures is not established.
- **start_semantics** — The entry image clears bit 15 of 0x45000100 to start this context and this image clears bit 15 of 0x40022000 in its init opcode. The two are the same idiom on different registers; neither register is identified.

## Checks

- PASS — both preserved source hashes match the allowlist (installed and vendor)
- PASS — the imported slice is exactly vendor[0x74000:0x7c000] (0c718c523ef24e07)
- PASS — the range is 0x8000 bytes (0x8000)
- PASS — the image is byte-identical across the two releases (4 differing bytes, all in the word-sum field)
- PASS — every differing byte lies in the application word-sum field (0x7bffc)
- PASS — the vector table extends past the core slots (73 entries, 57 external)
- PASS — the initial SP and reset vector match the recorded values (0x1803e458 / 0x180381c1)
- PASS — most external slots share one default handler (56 slots -> 0x180381f7)
- PASS — exactly one external interrupt has its own handler (IRQ3)
- PASS — the handshake token really is at the recorded offset (0x1803b214 = 0x12345678)
- PASS — the token is loaded from a literal pool, not computed (pool 0x3214)
- PASS — the opcode dispatch table has sixteen entries (16)
- PASS — most opcodes have a distinct handler (10 implemented, 6 fall through)
- PASS — the census reports both resolved and unresolved accesses (353 resolved, 484 unresolved)
- PASS — no RESOLVED access reaches application RAM (0x18000000..0x18037fff)
- PASS — the 0x40040000 block is exclusive to this image (5 exclusive windows)
- PASS — a write-strobe-readback register trio is present (a write-strobe-readback trio driven by an index)
- PASS — 0x40022000 is NOT used as a per-channel bank in this image (1 register)
- PASS — no DMA descriptor structure was resolved (stated as a bounded negative)
- PASS — this image runs log 114's reset-path disable on BOTH watchdog blocks (0x5afa0000 -> +0, 0x5afa55aa -> +0xc)
- PASS — the travel array address appears in NO aligned word of any image (log 110's negative survives this image)
- PASS — the token is found inside this image (as log 113 predicted)
- PASS — the record array address is known to BOTH sides (which is what makes it a mailbox rather than a coincidence)
- PASS — the Hall gate is reported as narrowed and NOT closed (narrowed, not closed)
- PASS — the gate is not claimed closed on an image that does not fill the array (the boundary is stated more precisely instead)
- PASS — every required question has an answer (6 claims)
- PASS — every claim carries a known confidence label
- PASS — every claim carries a kind_basis and at least one citation
- PASS — the unresolved boundaries are enumerated (6 entries)
