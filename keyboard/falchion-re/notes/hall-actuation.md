# Hall-effect actuation model (Phase 5D)

Generated by `tool/model_hall_actuation.py`. Do not edit by hand.

## The decision

```
travel >= 100        -> key down
travel == 0          -> key up
1 <= travel <= 99    -> UNCHANGED (hold band)
key id 0x00 or 0xd3  -> skipped before any comparison
```

## Pipeline status

| stage | status |
|---|---|
| acquisition | unresolved |
| calibration | unresolved |
| filtering | unresolved |
| position_travel | unresolved — the bytes are already scaled |
| comparison | observed |
| per_key_state | observed |
| hid | observed (log 109) |

## Findings

| finding | confidence | verified against |
|---|---|---|
| a travel byte at or above 100 sets the key's bit | observed | listing |
| a travel byte in 1..99 leaves the bit UNCHANGED | observed | listing |
| only an exact 0 clears the key's bit | observed | decompiler |
| key ids 0x0 and 0xd3 are skipped before any comparison | observed | listing |
| the key map is 5 groups x 15 = 75 entries per layer | observed | listing |
| the ACTIVE inner count is a runtime word at region+0x55c, not a static constant | observed | bytes |
| the per-key actuation setting is 7 bits at +8 of a 0x20-byte per-key record, with bit 15 selecting per-key over the global default | strongly-inferred | decompiler |
| the setting is clamped: below 2 -> 0, 2..4 -> value-2, 5 or more -> 3 | observed | listing |
| a second branch reports travel/5 with the key id rather than a binary state | strongly-inferred | decompiler |
| the comparison runs on the every-eighth-tick branch of IRQ38's prescaler | strongly-inferred | xref |
| the producer of the travel bytes is NOT RECOVERED | unresolved | xref |
| no MMIO block in either image has an ADC shape | observed | xref |
| no calibration table, baseline/min/max array or filter is recovered | unresolved | decompiler |
| no raw-to-travel conversion, sensor polarity, voltage bound, noise margin or physical distance is claimed | unresolved | decompiler |

## Constants

| name | value |
|---|---|
| `actuate_at` | `100` |
| `actuation_clamp` | `{'high': 5, 'low': 2, 'max': 3}` |
| `actuation_field_mask` | `127` |
| `group_stride` | `15` |
| `layer_stride` | `75` |
| `outer_groups` | `5` |
| `per_key_record_stride` | `32` |
| `profile_bank_stride` | `3460` |
| `skip_key_ids` | `[0, 211]` |
| `travel_report_divisor` | `5` |
| `vendor_actuation_divisor` | `10` |

## Physical interpretation

NOT ESTABLISHED and not establishable from static code: sensor polarity, voltage limits, noise margin, physical travel distance, and any safe scan rate. The recovered numbers carry no unit.

**This offline analysis authorises NO custom-firmware Hall drive and NO live experiment.**

## Checks

- PASS — the layer stride is the product of the two recovered strides (5 x 15 = 75 = 0x4b)
- PASS — every finding carries a confidence from the closed set
- PASS — every finding says what it was verified against
- PASS — the threshold and geometry claims are listing-verified
- PASS — acquisition, calibration and physical units stay unresolved
- PASS — no physical interpretation is recorded
- PASS — the model authorises no hardware action
- PASS — a travel byte at the threshold actuates
- PASS — a travel byte one below the threshold does NOT actuate a released key
- PASS — the hold band keeps a pressed key pressed
- PASS — only zero releases
- PASS — the actuation clamp matches the recovered branches ([0, 0, 0, 1, 2, 3, 3, 3])
- PASS — every application address is looked up, not computed (the comparison function and the edge detector both have measured counterparts)
- PASS — the two application functions relocate differently, which is why a rule would be wrong (0x18004a7e -> 0x18004a7e (delta 0) but 0x18005a88 -> 0x18005a5c (delta 0x2c), and both sit above the insertion point)
- PASS — the comparison function's cross-release match is only TENTATIVE, and that is recorded rather than smoothed over (FUN_18004a7e match confidence = tentative)
