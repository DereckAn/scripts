# Command-to-log manifest

The investigation began on 2026-08-29 and continued through 2026-09-02. Each
row records its own scope. Device-facing firmware preservation used only USB
READ operations plus separately authorized reset-only bootloader entries; no
erase, program, unlock, update, or SPI command was sent. Shell loops only
aggregate kernel/sysfs metadata.

| Log | Command or command group | Result / note |
|---|---|---|
| `00-host-context.txt` | `date --iso-8601=seconds`; `uname -a`; `cat /etc/os-release` | Host context |
| `01-lsusb.txt` | `lsusb` | Failed in sandbox: `unable to initialize libusb: -99` |
| `02-lsusb-tree.txt` | `lsusb -t` | Kernel USB topology and bindings |
| `03-tool-availability.txt` | `command -v` for USB/HID/fwupd tools; `lsusb --version`; `dfu-util --version`; `fwupdmgr --version` | fwupd tools absent |
| `04-usb-sysfs-devices.txt` | Loop over `/sys/bus/usb/devices/*`, reading device attributes | Found Falchion at `6-2`, `0b05:1b7e` |
| `05-usb-sysfs-interfaces.txt` | Loop over USB interface sysfs attributes and driver symlinks | Five HID interfaces; interface 4 unbound |
| `06-hidraw-sysfs.txt` | Loop over `/sys/class/hidraw/hidraw*`, reading links and `uevent` | Maps hidraw0–3 to Falchion interfaces 0–3; exit 1 because sandbox hides `/dev` nodes |
| `07-falchion-usb-descriptors-sysfs-xxd.txt` | `xxd -g 1 /sys/bus/usb/devices/6-2/descriptors` | Kernel-cached raw USB descriptors |
| `08-falchion-endpoints-sysfs.txt` | Loop over `6-2:1.*/ep_*` endpoint attributes | Endpoint layout |
| `09-falchion-hid-report-descriptors-xxd.txt` | `xxd -g 1` on hidraw0–3 sysfs `report_descriptor` files | Cached report descriptors for bound interfaces |
| `10-falchion-udev-properties.txt` | `udevadm info --query=all` for hidraw0–3 and USB device 6-2 | Existing udev properties only |
| `11-dfu-util-list.txt` | `dfu-util -l` | Sandbox attempt failed at libusb initialization; not a detection result |
| `12-fwupd-availability.txt` | `command -v`; `pacman -Q fwupd`; `systemctl status fwupd.service` | Package/binaries absent; system bus restricted |
| `13-interface-4-and-kernel-log.txt` | Read interface 4 `uevent`; query relevant `journalctl -k -b` lines | Kernel rejected OUT-only HID interface for lack of input interrupt endpoint |
| `14-usbhid-dump-help.txt` | `usbhid-dump --help` | Help only; no device access |
| `15-lsusb-falchion-verbose.txt` | `lsusb -d 0b05:1b7e -v` | Approved direct read-only descriptor/status queries; success |
| `16-dfu-util-list-direct.txt` | `dfu-util -l` | Approved direct read-only enumeration; success, no DFU targets listed |
| `17-prior-report-descriptor-comparison.txt` | `sed`, `wc`, and `sha256sum` on prior repository captures | Historical descriptor comparison |
| `18-device-node-metadata.txt` | `ls -l` and `stat` on expected `/dev` nodes | Nodes hidden by sandbox; no permissions changed |
| `19-port-retry-sysfs-devices.txt` | Read ASUS USB device attributes from sysfs after reconnecting | Falchion re-enumerated as bus 006 device 008 |
| `20-port-retry-sysfs-interfaces.txt` | Read ASUS USB interface attributes and driver links from sysfs | Same five HID interfaces and bindings |
| `21-port-retry-lsusb-tree.txt` | `lsusb -t` | Same topology/interface layout; new transient address |
| `22-port-retry-lsusb-verbose.txt` | `lsusb -d 0b05:1b7e -v` | Approved direct read-only descriptor/status retry |
| `23-port-retry-dfu-util-list.txt` | `dfu-util -l` | Approved direct read-only retry; no DFU target |
| `24-port-retry-cached-descriptors.txt` | Hash/hex-dump cached USB and Falchion HID report descriptors | Fresh byte evidence |
| `25-port-retry-comparison.txt` | Initial text reconstruction and diff | USB `DIFFERENT` line is a parser false positive; superseded |
| `26-port-retry-corrected-comparison.txt` | Strict two-hex-digit extraction and comparison | USB and interfaces 0–3 report descriptors identical; verbose diff only address |
| `27-claude-notes-report-desc-provenance.txt` | Reconstruct old `report-desc-0.txt` bytes and compare against all current cached hidraw descriptors | No match; old interface-4 attribution is unsupported |
| `28-claude-progress-audit.txt` | Inventory Falchion files/commits/artifacts/tools and locate safety-sensitive guide lines | Earlier progress and missing work audit |
| `29-asus-package-original-metadata.txt` | `stat`, `file`, and `sha256sum` on the untouched ASUS ZIP | Hash exactly matches ASUS publication |
| `30-asus-package-archive-list.txt` | `7z l -slt` on the ASUS ZIP | Read-only archive inventory |
| `31-asus-package-extracted-inventory.txt` | `find`, `file`, and `sha256sum` over the extracted package | Full extracted-file inventory |
| `32-asus-package-firmware-focused-inventory.txt` | Inventory/hash firmware subtree and read INI/version metadata | Located 1.00.58 BIN and bootloader PID 1b7f |
| `33-asus-firmware-image-static-analysis.txt` | `xxd`, `strings`, signature search, byte histogram, entropy/compression estimates | Static firmware-format evidence; no execution |
| `34-asus-updater-and-container-static-analysis.txt` | Marker/offset mapping plus `objdump` and `strings` on updater components | Proprietary HID erase/program updater identified |
| `35-official-artifact-preservation.txt` | `mkdir -p`; non-overwriting `cp --preserve=timestamps`; `cmp --silent`; `stat`; `sha256sum` | Official ZIP and 1.00.58 BIN copied locally and verified byte-for-byte; no keyboard access |
| `36-firmware-layout-analyzer.txt` | `python3 keyboard/falchion-re/tool/analyze_sonix_firmware.py` | Reproducible read-only container, CRC, vector, duplicate-region, and fill map |
| `37-firmware-modification-feasibility.txt` | `xxd`, `strings`, `objdump`, `llvm-mc`, temporary `/tmp` ELF wrapping, Python CRC/pointer scans, and SONiX-domain web searches | Dual Cortex-M3 roles, plain USB identity tables, updater integrity behavior, and modification blockers |
| `38-ghidra-preinstall-check.txt` | `command -v`; `pacman -Si`; `pacman -Q`; `java -version`; `archlinux-java status`; official Ghidra/Arch documentation search | Ghidra and JDK package availability checked; nothing installed |
| `39-ghidra-install-verification.txt` | `pacman -Q`; `command -v`; `readlink -f`; JDK 21 `java -version` | Verified installed Ghidra 12.1.2-2.1, headless launcher, and JDK 21.0.11 |
| `40-ghidra-seed-entries.txt` | `ghidra-analyzeHeadless -process ... -noanalysis -postScript FalchionSeedEntries.java` for four derived programs | Added only verified entry/vector labels to the local project; created missing candidate A/B entry functions; source BIN and keyboard untouched |
| `41-ghidra-entry-reanalysis.txt` | `ghidra-analyzeHeadless -process ...` for candidates A and B | Reanalysis completed and saved; one isolated candidate-B decode warning recorded |
| `42-ghidra-project-report.txt` | `ghidra-analyzeHeadless -process ... -readOnly -noanalysis -postScript FalchionProjectReport.java` for four programs | Verified language, memory blocks, entry recognition, function/instruction counts, and relevant strings |
| `43-firmware-layout-with-ram-image.txt` | `python3 tool/analyze_sonix_firmware.py` | Read-only parser rerun after adding the verified `0x74000` RAM-image vector |
| `44-ghidra-candidate-b-label-correction.txt` | `ghidra-analyzeHeadless -process app_candidate_b.bin -noanalysis -postScript FalchionSeedEntries.java` | Renamed the provisional local Ghidra function from `CandidateB_Entry` to the evidence-bounded `CandidateB_Start_Function`; source BIN and keyboard untouched |
| `45-ghidra-synchronized-project-report.txt` | `ghidra-analyzeHeadless -process ... -readOnly -noanalysis -postScript FalchionProjectReport.java` for four programs | Confirmed synchronized analysis labels and memory mappings in explicit read-only mode |
| `46-documentation-synchronization-audit.txt` | `date`; `git status`; firmware SHA-256; read-only analyzer; active-document stale-claim scan; file-presence checks | Post-pull documentation/evidence synchronization audit; no keyboard access |
| `47-ghidra-candidate-b-opcode-search.txt` | `ghidra-analyzeHeadless ... -readOnly -noanalysis -postScript FalchionOpcodeSearch.java` | Located opcode comparisons and candidate dispatcher paths offline |
| `48-ghidra-candidate-b-dispatcher-report.txt` | `ghidra-analyzeHeadless ... -readOnly -noanalysis -postScript FalchionDispatcherReport.java` | Disassembled/decompiled Candidate B vendor-HID branches, including `0x50/0x55` and `0x51` |
| `49-ghidra-candidate-b-key-remap-report.txt` | `ghidra-analyzeHeadless ... -readOnly -noanalysis -postScript FalchionKeyRemapReport.java` | Continuous `0x51/0x21-0x22` handler, RAM literals, callers, and response helper report |
| `50-firmware-pointer-byte-search.txt` | Exact offline binary search using Perl `pack(H*)` and `index()` | Located RAM-pointer literals in the preserved BIN/derived slices; no dispatcher pointer found |
| `51-ghidra-key-ram-reference-report.txt` | Read-only Ghidra direct-reference scan for Candidates A and B | Classified direct reads/writes to key configuration, policy, profile, and HID-buffer RAM regions |
| `52-ghidra-candidate-b-key-config-init-report.txt` | Read-only Ghidra decompilation of key configuration initializers/consumers | Identified configuration-load paths and `R_NSK_M` / `R_NSK_FnM` skip behavior |
| `53-ghidra-candidate-b-reserved-key-gate-report.txt` | `ghidra-analyzeHeadless ... -readOnly -noanalysis -postScript FalchionReservedKeyGateReport.java` | Verified the 6-entry versus 57-entry runtime unsupported-key predicate |
| `54-ghidra-protocol-labels.txt` | Apply `FalchionApplyProtocolLabels.java`, then repeat with `-readOnly` | Saved and independently confirmed three evidence-supported labels in the ignored local Ghidra database |
| `55-offline-protocol-analysis-audit.txt` | `git diff --check`; source BIN hash; evidence/stale-question searches; new-log counts; unhashed-log inventory; `git status` | Verified documentation consistency and enumerated logs awaiting manifest hashes; no keyboard access |
| `56-timeline-document-audit.txt` | Timeline size/section/fact-anchor checks; referenced-file existence; `git diff --check`; source BIN hash; `sha256sum -c`; `git status` | Verified the chronological review document and existing evidence integrity; no keyboard access |
| `57-ghidra-runtime-key-table-reference-scan.txt` | Updated `FalchionRamTableReferences.java`; read-only scan of four Ghidra programs | Added the exact unsupported-list range; base-zero programs did not resolve its indirect pointer |
| `58-ghidra-runtime-key-table-xrefs.txt` | `FalchionRuntimeTableXrefs.java` read-only across four programs | Searched incoming code/data references to translation, index, and policy ranges |
| `59-ghidra-startup-and-copy-report.txt` | `FalchionStartupAndCopyReport.java` read-only across four programs | Compared start/reset routines and memcpy-like callers; showed Candidate B offset zero is a callback-like function, not proven startup |
| `60-candidate-b-runtime-table-extraction.txt` | `xxd` plus little-endian Perl decoding at Candidate B offsets derived from runtime pointers | Recovered the 189-byte translation table, index-map sample, and exact 6+57 policy words |
| `61-candidate-b-table-analysis.txt` | `python3 tool/analyze_candidate_b_tables.py` | Reproducibly mapped runtime/file offsets, decoded 68 source translations, and named both policy lists |
| `62-ghidra-candidate-b-runtime-base-import.txt` | Byte-identical derived copy; Ghidra binary import at `0x18000000` | Created a separate corrected-base analysis program; official BIN and original provisional program untouched |
| `63-ghidra-candidate-b-runtime-base-validation.txt` | Seed/label corrected-base program, then read-only project/RAM/xref reports | Verified memory block `0x18000000-0x1801e753` and internal table/policy references |
| `64-ghidra-candidate-b-runtime-dispatcher-seed.txt` | Seed dispatcher, reanalyze, apply labels, read-only confirm | Reconstructed and labeled dispatcher at runtime `0x18001fbe`; retained known warning at `0x180024f2` |
| `65-ghidra-candidate-b-rebased-key-policy-report.txt` | Base-aware remap and reserved-key reports in read-only mode | Confirmed corrected runtime addresses and direct references to embedded tables |
| `66-candidate-b-runtime-mapping-audit.txt` | Analyzer reproduction; hashes/copy comparison; policy counts; evidence/stale-claim scans; `git diff --check`; manifest inventory | Verified corrected mapping, exact 6+57 row counts, synchronized documentation, and unchanged artifacts |
| `67-ghidra-candidate-b-kbid-map-report.txt` | `FalchionKbidMapReport.java` against corrected-base Candidate B with `-readOnly -noanalysis` | First-pass KBID/table report; its eight-chunk visual split was superseded after code proved overlapping wire windows and a separate scan map |
| `68-candidate-b-kbid-layout-analysis.txt` | `python3 tool/analyze_candidate_b_tables.py dumps/vendor/M605_V01_00_58.bin` | Deterministically decoded Candidate A's 26-byte KBID lookup, three logical wire windows, target translation, record addresses, and three scan rows |
| `69-ghidra-candidate-b-corrected-kbid-map-report.txt` | Corrected `FalchionKbidMapReport.java` with `-readOnly -noanalysis` | Verified three `0xbd` logical windows at `0x86` stride, shared 55-byte region, three `0x100` scan rows, and KBID-selection code |
| `70-ghidra-corrected-key-map-reference-scan.txt` | Corrected `FalchionRamTableReferences.java` and `FalchionRuntimeTableXrefs.java` against corrected-base Candidate B with `-readOnly -noanalysis` | Verified the wire-only, shared wire/scan, and scan-only boundaries and their direct references |
| `71-kbid-layout-analysis-audit.txt` | Analyzer reproduction; source hash; existing log-hash verification; stale-claim/boundary scans; `git diff --check`; manifest inventory | Final local-only consistency and evidence audit for the KBID/layout-map pass |
| `72-ghidra-candidate-a-loader-report.txt` | `ghidra-analyzeHeadless -process app_candidate_a.bin -readOnly -noanalysis -postScript FalchionCandidateALoaderReport.java` | Recovered Candidate A reset handler, `__scatterload` region table at `0x5750`, and clock/stack-guard init `FUN_00001216`; read-only project discarded |
| `73-ghidra-candidate-a-scatter-handler-report.txt` | Same script/invocation with ephemeral disassembly of the indirect scatter handlers | Confirmed the `0x17c` decompress, `0x1d8` copy, and `0x1f4` zero-init handlers and the `0x60021000 → 0x18000000` image copy corroborating the runtime base |
| `74-candidate-integrity-crc-analysis.txt` | `python3 tool/analyze_candidate_integrity.py` (pre-resolution version) | Decoded the SN_FWIN 4-word record table, reproduced Candidate A's IEEE CRC-32 exactly, and showed Candidate B's `0x1a76c116` matches no plain-CRC-32 interpretation/range of the container's B bytes |
| `75-ghidra-bootloader-verify-report.txt` | `ghidra-analyzeHeadless -process bootloader_primary.bin -readOnly -noanalysis -postScript FalchionBootloaderVerifyReport.java` | Read-only decompile of the boot-selection/verify chain: `FUN_00005028` per-record checksum (sum of per-`0x10000`-chunk CRC-32), `FUN_000026d0` whole-region word-sum, and supporting `FUN_00008000`/`0000511c`/`00005240` |
| `76-candidate-integrity-resolved.txt` | `python3 tool/analyze_candidate_integrity.py` (resolved version) | Reproduced and asserted all four container integrity fields: A `0x5e75c17a`, B `0x1a76c116` (chunk-CRC sum), and word-sums `0xfb665ae3`/`0x5d27c5a9`; supersedes log 74 |
| `77-image-builder-roundtrip.txt` | `python3 tool/build_modified_image.py` | Offline image builder self-check: recompute of the unmodified BIN is idempotent, a naive one-byte Candidate B patch fails record+word-sum verification, and the rebuilt image reproduces all four integrity fields plus every boot-container invariant; preserved BIN untouched. **Conclusion superseded by log 84** — the run shows the fields are reproducible offline, not that the bootloader accepts the image or that it boots; `FUN_000029d4` and the top-level selected-entry comparison remain unresolved. Raw log unedited |
| `78-boot-structures.txt` | `python3 tool/analyze_boot_structures.py` | Decoded the SNC7320A/SN_BCFG/SN_FWIN boot chain (primary + backup containers, 2-slot boot-priority table, header field map) and asserted the boot-gate invariants a modified image must preserve to boot |
| `79-ghidra-candidate-a-handoff.txt` | `ghidra-analyzeHeadless -process app_candidate_a.bin -readOnly -noanalysis -postScript FalchionCandidateAHandoff.java` | Showed Candidate A's post-scatter runtime `FUN_000002c8` calls into Candidate B via the veneer `thunk_EXT_FUN_1800023a` (RAM `0x1800023a`) |
| `80-ghidra-candidate-b-entry.txt` | `ghidra-analyzeHeadless -process app_candidate_b_18000000.bin -readOnly -noanalysis -postScript FalchionCandidateBEntry.java` | Characterized Candidate B's true entry `0x1800023a` as application main ("welcome to main", HW/USB init, RTOS `INIT_TASK`, scheduler, dispatcher `0x18001fbe`) |
| `81-ghidra-bootloader-write-protocol.txt` | `ghidra-analyzeHeadless -process bootloader_primary.bin -readOnly -noanalysis -postScript FalchionBootloaderProtocol.java` | Decompiled the PID-1b7f vendor-HID write path: OUT-report dispatcher `FUN_00002db8` (cmd byte @report+0x34 → 0x01 erase / 0x05 read / 0x51 program), address guard `0x10000..0x7c000`, and the flash-controller primitives; cross-checked against updater strings in log 34 |
| `82-ghidra-bootloader-framing.txt` | `ghidra-analyzeHeadless -process bootloader_primary.bin -readOnly -noanalysis -postScript FalchionBootloaderFraming.java` | Decoded the exact 64-byte vendor-HID wire framing (router `FUN_0000bd40`, OUT parser `FUN_0000380c`, IN responder `FUN_00003740`): report[0]=sub-command, unlock `ASUSHIDFWU`, set-addr `0x20`/len `0x21`/data `0x22`/execute `0x1f`, READ guard `[0x10000,0x7bfff]` len≤0x30, and the force-boot flag `0x20000ffc`/magic `0x73207320` |
| `83-backup-tool-dryrun.txt` | `python3 tool/backup_firmware.py` (dry-run) | Read-only backup tool self-check: validated all 46080 dump-plan reports pass the read-only guard and that erase/program/unlock reports are unconstructable; no device opened. Host access separately confirmed (device `0b05:1b7e` present, `/dev/hidraw1-4` world-RW). **Conclusion superseded by log 84** — "unconstructable" is narrowed to "the guard rejected every write/unlock/reset form in the self-check", and this log is dry-run output only: it records no `--run` invocation, so the TIMELINE claim of a live `--run` refusal was withdrawn. Raw log unedited |
| `84-correction-audit.txt` | `python3 /tmp/gen_log84.py` — drives `py_compile`, `unittest discover -s tool`, `backup_firmware.py` (dry-run), `backup_firmware.py --run` (refused, no device), both analyzers at base 0 and at `--base 0x10000` over a temporary `/tmp` slice, and `build_modified_image.py --demo` | Audit and repair of commits `fdacad9..05c35ad`. Records the fatal query-batching defect in the backup tool (the `0x8f` status reply was consumed as `0xaa` read data), descriptor-validated node selection, end-of-dump self-validation, atomic no-overwrite publish, the corrected duplicate checksum offsets `0x0fffc`/`0x70ffc`, the withdrawn live-`--run` claim, and the unresolved post-EXEC scheduling race. 85 mocked tests pass; vendor SHA-256 reconfirmed; **the refusal path was exercised, but no device was accessed and the live path was never entered** |
| `85-bootloader-read-scheduling-analysis.txt` | `ghidra-analyzeHeadless ghidra/project falchion-hfx -process bootloader_primary.bin -readOnly -noanalysis -postScript` × 5 (`FalchionBootloaderReadScheduling`, `FalchionBootloaderReadContext`, `FalchionBootloaderTickProof`, `FalchionBootloaderIrqContext`, `FalchionBootloaderCallbackSlot`, `FalchionBootloaderEpTable`), then `py_compile`, `unittest discover -s $PWD/tool -t $PWD/tool` (the relative form recorded in log 84 is rejected by python 3.14's loader), `backup_firmware.py` (dry-run), `backup_firmware.py --run` (refused, no device), `git diff --check` | **Resolved the post-EXEC scheduling race: PROVEN POSSIBLE.** Resolved the RAM layout by literal-pool value rather than by offset (state `0x18012a8c`, transaction block `0x18011a8c`, service-loop flags `0x18010bd4`), enumerated every reader/writer of `state+0x34/0x35/0x36/0x38/0x39`, the READ-busy bit and the `0xaa` response buffer at `state+4`, and showed that `FUN_00002db8` is reachable only through `FUN_00003a7c`, whose loop body is gated on the SysTick flag that only the handler at `0x000048d0` writes — so polling `state+0x38` bit 1 cannot sequence a READ. No generation counter, address echo, completion byte or sequence number exists. `backup_firmware.py` replaced its busy-poll with a buffer-change handshake; 95 mocked tests pass; `--run` stays gated. **Partly superseded by log 86**: this log's "and the default outcome", "orders of magnitude", "microseconds" and "almost always" claims are withdrawn as unsupported by any recovered timing, and its replacement handshake was itself unsound (it read status before the data query, so it could accept a half-written buffer). Raw log unedited |
| `86-bootloader-read-handshake-correction.txt` | `ghidra-analyzeHeadless ghidra/project falchion-hfx -process bootloader_primary.bin -readOnly -noanalysis -postScript FalchionBootloaderStateZeroInit.java`, then `py_compile`, `unittest discover`, `backup_firmware.py` (dry-run), `backup_firmware.py --run` (refused, no device; `--force-unreviewed` **not** used), `sha256sum -c logs/SHA256SUMS`, `git diff --check` | **Correction of log 85.** Records the counterexample that makes log 85's handshake unsound (status read *before* the data query does not exclude a READ that starts and finishes between two reports, so a half-old/half-new buffer could be accepted — reproduced as 24 new + 24 baseline bytes). Adds the static evidence that closes the bootstrap: the reset path's `Region$$Table` entry at `0x0000ccc0` zero-initialises `0x18011168..0x1802b230` via `__scatterload_zeroinit`, covering `state+0x34`, `state+0x36`, `state+0x38` and `state+4`, so a freshly started bootloader has no pending operation and an all-zero response buffer. Gives the corrected rule (sample, **then** status, then a confirming fetch) with a full interleaving proof, bounds EXEC re-arming, and names the one residual the protocol cannot close (a foreign queued-but-undispatched READ). Withdraws log 85's timing overclaims. 108 mocked tests pass; no device accessed; `--run` still unauthorised |
| `87-bootloader-entry-recovery-and-preflight.txt` | Read-only Candidate B Ghidra trace; static .NET/native updater and HID-DLL parsing/disassembly; current sysfs descriptor inspection; `py_compile`; `unittest discover -s "$PWD/tool" -t "$PWD/tool"`; entry-tool dry-run/refusal; passive `ls`/`fuser`/selector preflight | Recovered and independently cross-checked the exact reset-only entry payload `7b aa 41 53 55 53 aa` + 57 zeros and Linux's leading zero report-number byte. Added an exact-frame-only entry tool and 7 tests (115 total). Passive preflight selected `/dev/hidraw7`; no node was opened and no report was sent. Live reset remains pending explicit approval |
| `88-live-bootloader-entry-and-passive-validation.txt` | After explicit approval: `python3 tool/enter_bootloader.py --run --acknowledge-reset`; then standard `lsusb`, sysfs descriptor/interface/endpoint inspection, `dfu-util -l`, bootloader selector, `ls -l`, `fuser`, and host-only fwupd availability checks | Sent exactly one reset-only application report; keyboard re-enumerated as `0b05:1b7f` bcdDevice 1.05. Passive validation selected interface 0 `/dev/hidraw6` (FF01, 64-byte IN/OUT, no Report ID). No bootloader report or flash operation sent. Nodes are root-only; next live query needs separate approval |
| `89-bootloader-split-channel-correction.txt` | First separately authorized probe (only `0x8f` sent before timeout); read-only Ghidra `FalchionBootloaderInterfaceMap.java` with `-readOnly -noanalysis`; passive `lsusb`, sysfs selector, `ls -l`, `getfacl`, `fuser`; `py_compile`; `unittest discover` | Corrected log 82's single-node inference: commands use FF01/EP6 and responses use distinct FF00/EP5. Shared split transport opens FF00 read-only and FF01 write-only; 130 offline tests pass. FF00 still lacks the owner's read ACL, so no corrected live retry occurred; no READ or flash operation sent |
| `90-live-split-channel-status-buffer-probe.txt` | After explicit approval: `python3 tool/probe_bootloader.py --run --acknowledge-volatile-length` | Exact four-report probe passed: FF01 commands, FF00 responses, locked/idle/error-free `0x0f` status before and after, and `0x2a` plus 48 zero bytes after the RAM-only length setter. No address, execute-READ, unlock, erase, program, reset, update, or SPI command |
| `91-one-block-read-validation.txt` | Offline `probe_flash_read.py` construction/dry-run and 136-test suite; passive mode/descriptor/ACL/holder checks; after separate approval one exact bootloader-entry reset; after narrow ACL restoration, `python3 tool/probe_flash_read.py --run --acknowledge-one-read` | Exactly one execute-READ returned a fresh complete 48-byte `SN_FWIN` block at `0x10000` (SHA-256 `5ed6cf84…5815`). First 44 bytes match vendor 1.00.58; final checksum u32 differs. No second address/READ, unlock, erase, program, update, or SPI command; no full backup |
| `92-full-app-region-backup.txt` | Offline dry-run and 136-test suite; physical power cycle; after separate approval one exact bootloader-entry reset; passive descriptor/ACL/holder checks; after narrow ACL restoration, `python3 -u tool/backup_firmware.py --run dumps/device/ROG_Falchion_Ace_HFX_installed_bcdDevice_1.59_app_0x10000_0x7bfff.bin --passes 3 --force-unreviewed`; independent `stat`, `sha256sum`, `analyze_candidate_integrity.py --base 0x10000`, and `analyze_boot_structures.py --base 0x10000` | Accepted a 442,368-byte application-region readback after three byte-identical passes, each SHA-256 `fc6128ab…37b`. Both record checksums, application word-sum, and all 12 applicable boot checks pass. READ only: no unlock, erase, program, update, persistent configuration, driver detach, or SPI operation. USB scope excludes the bootloader and the remainder of physical U5. |
| `93-step6-offline-custom-firmware-plan.txt` | Read-only inspection of the existing analyzer/builder interfaces, Ghidra map, current findings/timeline, and repository inventory; documentation edits only | Added `notes/step6-offline-custom-firmware-plan.md`: nine gated offline phases, strict device-safety boundary, deliverables and exit criteria, independent Codex review points, and ready-to-paste Claude Code prompts. No binary generated or modified; no device/network/privileged access. |
| `94-version-aware-image-format-library.txt` | `python3 -m py_compile` on the two new files; `python3 tool/falchion_image.py` on the vendor image (base 0) and on the installed dump (`--base 0x10000`), plus `--json`; a malformed-input fail-closed run; both existing analyzers at base 0 and `--base 0x10000`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after; `git diff --check`; `git status --short` | **Step 6 Phase 1.** Added `tool/falchion_image.py` (immutable `ImageView`/`Container`/`FwinHeader`/`Record`/`WordSum`/`Check`/`Layout`/`Validation` models, `parse`, `validate`, deterministic `to_dict`, and the `require_supported_source` SHA-256/base/size allowlist gate for mutation) and `tool/test_falchion_image.py` (38 tests). Offsets are logical flash offsets translated only in `ImageView.index`; record lengths are read per image. New facts: installed record[1] length `0x1e780` vs vendor `0x1e754` (+44 bytes, record[0] and both load addresses unchanged); the mirrored bootloader copy `[0x61000,0x71000)` validates its own additive word-sum `0xfb665ae3` at `0x70ffc` in both images; the SN_FWIN `+0x8` string is `v1.0.00` in both and is the container format version. No analyzer was refactored — four parity tests prove agreement instead. 174 offline tests pass; installed-dump results still match log 92; both evidence hashes unchanged; **no device was accessed** |
| `95-phase1-record-scan-correction.txt` | `grep` on `logs/75-ghidra-bootloader-verify-report.txt`; the reviewer's holed-table reproduction; `python3 tool/falchion_image.py` on both images; an eight-slot dump of both record tables; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; both analyzers at base 0 and `--base 0x10000`; repeated `--json` for determinism; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check`; `git status --short` | **Step 6 Phase 1 correction after independent review.** `FUN_0000511c` (log 75) scans exactly eight record slots and processes every slot whose length field is nonzero — there is no terminator. Log 94's parser stopped at the first zero address or zero length, so it dropped a nonzero-length record with a zero address, dropped active records behind a zero-length hole, and rejected a full eight-slot table. `parse_records` now scans all eight slots, skips only zero-length holes, preserves physical slot indices, bounds-checks every active slot, and fails closed on a zero address with nonzero length, a truncated table, or no active slot. Results for both preserved images are unchanged and still match log 92. `analyze_candidate_integrity.py`, `analyze_boot_structures.py` and `build_modified_image.py` still carry the terminator assumption; a test pins the divergence and reconciling them is a Phase 7 prerequisite. Also corrected the log-94 row's test counts from 37/173 to 38/174. 179 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `96-installed-vs-vendor-comparison.txt` | `python3 -m py_compile` on the two new files; `python3 tool/compare_firmware_images.py` and `--json`, with `sed` section extracts; a wrong-base fail-closed run; the default source refusal and the `--analysis-only` warning; writing `notes/installed-vs-vendor.md` and `.json` plus `wc -c` and `sha256sum` on both; a Markdown/JSON count cross-check; two `--json | sha256sum` runs for determinism; both analyzers and `falchion_image.py` at base 0 and `--base 0x10000`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check`; `git status --short` | **Step 6 Phase 2.** Added `tool/compare_firmware_images.py` (built on the Phase-1 parser, no second SN_FWIN parser; compares over logical `[0x10000,0x7c000)` only; refuses non-allowlisted inputs unless `--analysis-only`; one model rendered as Markdown and complete deterministic JSON) and `tool/test_compare_firmware_images.py` (41 tests). Facts: the images differ in **101,297 of 442,368 bytes (22.90%)** across **3,509** contiguous ranges and **39 of 108** `0x1000` pages, first difference `0x1002c`; every change is confined to `[0x10000,0x17000)`, `[0x21000,0x40000)` and `[0x7b000,0x7c000)`, so the container/bootloader-copy area `[0x60000,0x71000)`, both fill regions and the RAM image `[0x74000,0x79000)` are identical; record slot 0 keeps length `0x58ac` and differs in 131 bytes over 106 ranges while slot 1 grows `0x1e754`→`0x1e780` (+44) and differs in 101,112 bytes over 3,399 ranges, neither record moved; the installed mirrored bootloader copy matches the vendor backup and primary ranges with zero differing ranges; and all **603** ASCII strings are present in both images with nothing added, removed or rewritten. No meaning is assigned to any changed range. 220 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `97-phase2-review-corrections.txt` | Reviewer reproductions for a changed record destination, a `compare()` tripwire, and `--analysis-only --json`; a duplicate-occurrence string case; `python3 tool/compare_firmware_images.py` with `sed` section extracts; regenerating and hashing both note artifacts; a Markdown/JSON count cross-check; two `--json | sha256sum` runs; `py_compile`; both analyzers and `falchion_image.py` at base 0 and `--base 0x10000`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check`; `git status --short` | **Step 6 Phase 2 corrections after independent review.** Five findings fixed. (1) Record `dst` was parsed but absent from Markdown and JSON, so a runtime-load-address change could go unreported; `RecordCompare` now carries both destinations plus `addr_changed`/`dst_changed`/`checksum_changed`. (2) Provenance was checked after `compare()`, against the plan's "verify both tuples before analysis"; the allowlist gate now runs before any parsing, hashing or diffing, pinned by a tripwire test. (3) `--analysis-only --json` printed the warning to stdout and produced unparseable JSON; the warning moved to stderr and the waiver is recorded under a `provenance` key. (4) Strings were compared as sets and mis-described as "603 ASCII runs"; they are now compared as multisets and reported as **603 distinct values in 802 occurrences**, with `count_changed` so a duplicate-count change cannot hide. (5) Slot 1's extra 44 bytes are now the explicit installed-only span `0x3f754..0x3f780` instead of a scalar delta. Every log-96 measurement is unchanged. 232 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `98-installed-code-map-and-function-matching.txt` | `py_compile`; `python3 tool/extract_installed_records.py` on the installed dump and on the vendor BIN, plus `--write` and `--json`; a slice round-trip proof; four `ghidra-analyzeHeadless -import` runs into the new `ghidra/project-step6`; four `-process <slice> -readOnly -noanalysis -postScript FalchionFunctionInventory.java` runs; `python3 tool/match_functions.py` for both program pairs (text and `--json`); aligned-versus-raw change totals; regenerating both notes and hashing them twice; both analyzers, `falchion_image.py` and `compare_firmware_images.py`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check`; `git status --short` | **Step 6 Phase 3.** Added `tool/extract_installed_records.py` (26 tests), `tool/match_functions.py` (22 tests), `ghidra/scripts/FalchionFunctionInventory.java`, `notes/installed-record-load-map.md` and `notes/vendor-to-installed-functions.md`. The runtime map is derived from installed bytes only: Candidate A's scatter-region table is located by structure at flash `0x16750`, giving copy `0x21000..0x3f380`→RAM `0x18000000..0x1801e380`, decompress `0x3f380..0x3f780` (`0x400` in) → `0x1801e380..0x1801ee84` (`0xb04` out), and zeroinit → `0x18036168`, whose end equals the entry image's initial SP in both releases. Record slot 1 = copy + `0x400` exactly in both, so the +44 growth is entirely in the copy region. Both program pairs are fully matched with nothing unmatched: Candidate A 80/80 (76 identical, 2 structural, 2 tentative, shift `0`), Candidate B 293/293 (236 identical, 48 structural, 9 tentative, uniform shift `+0x2c` measured from the confident matches). Aligning the gaps shows the real change is **131 bytes** in A and **1,232 bytes plus one 44-byte insertion** in B, against log 96's raw 101,112 — the rest was relocation. The insertion site is the single unaligned data gap, vendor `0x180047fc..0x180057d2` vs installed `0x180047fc..0x180057fe`, outside every function body. The pre-existing Ghidra project was not opened or modified. No function meaning is inferred. 280 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `99-phase3-review-corrections.txt` | A discontiguous-body census over the regenerated inventories; `grep` on the fixed `FalchionFunctionInventory.java`; `python3 tool/match_functions.py` for both pairs; aligned-versus-raw totals; `python3 tool/extract_installed_records.py` text and `--json`; a write-refusal reproduction; `python3 tool/report_phase3.py` and `--check`; `wc -c`/`sha256sum` on the five note artifacts; a per-record full-extraction proof; `py_compile`; both analyzers, `falchion_image.py` and `compare_firmware_images.py`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check`; `git status --short` | **Step 6 Phase 3 corrections after independent review.** Five findings fixed. (1) Ghidra function bodies can be discontiguous and **15 of 80 / 61 of 293 here are**; the inventory script now iterates `body.getAddressRanges(true)`, hashes the concatenated real ranges and emits a `ranges=` field, and the matcher parses it, refuses an inventory without it or whose ranges disagree with `size`, diffs bodies only when range shapes match, and derives data spans from the complement of the union of real ranges. All four inventories and every number were regenerated: **Candidate A now has zero differing function-body bytes** (all 131 changed bytes are data, 78 identical / 0 structural / 2 tentative), Candidate B is 253 body + 977 data = **1,230** (260/24/9), and the insertion site is vendor `0x180047f8..0x180057d2` vs installed `0x180047f8..0x180057fe`. (2) Each active SN_FWIN record is now extracted **whole** — slot 1 `0x21000..0x3f780`, not just the loadable `0x21000..0x3f380` — with a new check that every active record byte is covered by a slice. (3) `tool/report_phase3.py` is checked in and renders both notes plus three JSON artifacts including the complete 373-pairing mapping, with a `--check` mode; the ephemeral `/tmp` generator is gone. (4) The "address is never used to match" claim is corrected to "never the *sole* signal", since one tentative rule uses the measured shift together with byte equality. (5) `--write` is refused unless every check passes. The scatter/load map is unchanged. 303 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `100-installed-hardware-interface-map.txt` | Four `ghidra-analyzeHeadless ... -postScript FalchionSeedVectors.java <Name=0xaddr>...` seeding runs (write, ignored project only), four re-analysis passes, then eight `-readOnly -noanalysis` report runs regenerating every inventory and peripheral map; `python3 tool/map_hardware_interfaces.py` text and `--json` twice for determinism; `python3 tool/report_phase5.py` and `--check`; `python3 tool/report_phase3.py --check`; `python3 tool/match_functions.py --json` for both pairs; `py_compile`; both analyzers, `falchion_image.py`, `extract_installed_records.py` and `compare_firmware_images.py`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check`; `git status --short` | **Step 6 Phase 5** (executed at the owner's direction with Phase 4 still undone; Phase 5 does not depend on it). Added `ghidra/scripts/FalchionPeripheralMap.java` (MMIO census via Ghidra constant propagation), `ghidra/scripts/FalchionSeedVectors.java`, `tool/map_hardware_interfaces.py` (29 tests), `tool/report_phase5.py`, and `notes/installed-hardware-interfaces.{md,json}`. A raw binary import leaves handlers undisassembled, so the vector targets were seeded in **both** releases from each one's own table; the function set grew 80→97 and 293→530 per side. Findings: the table is 73 slots at `0x0..0x124` (16 core + 57 external) bounded by the default handler `0x14df`, first code at `0x140`; all six fault vectors plus SVCall/PendSV/SysTick are populated; eight external IRQs carry non-default handlers (IRQ3 in the entry image, IRQ6/31/32/36/37/38/48 in the application); software enables exactly **IRQ6 and IRQ38** via NVIC_ISER0/1, cross-checking the table; NMI writes AIRCR `0x05fa0004` (**SYSRESETREQ**); SysTick writes ICSR `0x10000000` (**PENDSVSET**) and the application writes it 14 more times; HardFault reads CFSR/MMFAR/BFAR; two identical unnamed blocks at `0x40008000`/`0x40009000` take a `0x5afa` magic key on the reset path; reset-path register `0x4002f004` is written `0x60021000`, record slot 1's address; and the unnamed `0x40100000` block (19 registers, 126 accesses) is the one IRQ6 serves. **No vendor peripheral is named** — USB, GPIO/scan, Hall-effect/ADC, RGB and the nonvolatile write path are declared *not-covered* for want of a SNC73270 manual. Exit gate met: a dependency map gives must-replace / must-replace-if-service-needed / may-omit per block by stated rule. Seeding also forced a **Phase 3 regeneration**, which exposed and fixed a real flaw: data spans were paired by list index and drifted; they are now keyed by the preceding matched function and compared only when key, distance and length all agree. That also **corrects logs 98/99**: slot 1's 44-byte growth is distributed across bodies and spans, not one insertion. 337 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `101-boot-acceptance-resolved.txt` | A provenance proof that the installed dump's mirrored bootloader copy equals the vendor primary; `ghidra-analyzeHeadless -import` plus `FalchionSeedVectors.java` and re-analysis for the bootloader slice and the RAM handoff routine in `ghidra/project-step6`; `-readOnly -noanalysis -postScript FalchionDecompileTargets.java` on `0x29d4 0x2a44 0x7fa8 0xffc 0x5272 0x2d4a` and on `0x18010000`; a literal-pool read; the bootloader scatter-table read; a crypto-constant, string and USB-identity search; `python3 tool/analyze_boot_acceptance.py` text and `--json` twice; `python3 tool/report_phase4.py` and `--check`; `report_phase3.py --check` and `report_phase5.py --check`; `py_compile`; the analyzers, `falchion_image.py`, `extract_installed_records.py`, `map_hardware_interfaces.py` and `compare_firmware_images.py`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check`; `git status --short` | **Step 6 Phase 4, exit gate met.** Added `ghidra/scripts/FalchionDecompileTargets.java`, `tool/analyze_boot_acceptance.py` (34 tests), `tool/report_phase4.py` and `notes/boot-acceptance-conditions.{md,json}`. Analysed the bootloader **out of the device dump's mirrored copy** (`c244aef0…`, proven equal to the vendor primary). **Both named unknowns resolved.** `FUN_000029d4` is a *recovery key-combination poll*, not an integrity check: it ticks the bootloader's own scan and returns 1 — blocking the boot — once RAM `0x18012ac8` shows `+0x0 == 0xa0` and `+0x10 == 0x100` for 30 consecutive samples. The top-level comparison is against `DAT_00007f98` = **`0x60011000`**, so the SN_FWIN `+0x10` entry pointer must equal that constant exactly and **the entry image cannot be relocated**. A previously unnamed third gate, `FUN_00002a44`, reads `0x20000ffc` and compares it with `0x73207320` (`" s s"`), clearing it when matched — a one-shot software bootloader-entry flag matching logs 87/88. `FUN_000026d0(0x6c000)` sums from `DAT_0000277c` = `0x60010000`, i.e. exactly the application region. **The handoff is a copy and a reset, not a branch**: `BootHandoff`, scatter-loaded from program `0xcdfc` to RAM `0x18010000`, masks interrupts, copies a fixed `0x10000` bytes from the entry to **address 0**, writes AIRCR VECTKEY|SYSRESETREQ and spins — which explains why Candidate A is linked at 0, why record `+0xc` is not a load destination, and why the `0xff` fill sits inside the copied window. Searched and **not found**: any cryptographic constant, any version/signature/key/auth/rollback string, any device-ID gate, any configuration gate. Two proven checks were added to `falchion_image` and `analyze_boot_structures` (entry-pointer constant, handoff window), moving their counts to 20/14 and superseding log 92's; and the two boot-gate items left every `UNRESOLVED` list. 373 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `102-phase4-and-phase5-review-corrections.txt` | Byte-flip reproductions against both the Phase 4 verdict and Phase 1 validation; `sed`/`grep` on the stored decompiles for the handoff and the recovery counter; a vector-table word dump around `0x13c`; `classify_address` over eight probe addresses; an oversized-record reproduction for the replaced check; a register-schema census over the Phase 5 JSON; a span-flag check over the Phase 3 JSON; `python3 tool/report_phase4.py` and `report_phase5.py` plus `--check` on phases 3, 4 and 5; two `--json | sha256sum` runs each for the boot-acceptance and hardware tools; `py_compile`; the analyzers, `falchion_image.py`, `extract_installed_records.py` and `compare_firmware_images.py`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check` on worktree and index; `git status --short` | **Phase 4 and Phase 5 corrections after independent review — ten findings, three of them blockers.** (1) `analyze_boot_acceptance`'s verdict never executed the integrity rules it reported, so a flipped application byte passed; every `falchion_image.validate` check and word-sum is now folded in (8 checks → 28) and the verdict is renamed `image_rules_ok` with a `RESULT_MEANING` line, since the two environmental gates cannot be judged from an image. (2) `FUN_00007fa8` dereferences VTOR before adding `0x1c`, so the entry lands at **`*(VTOR) + 0x1c`**, not the `0xe000ed24` log 101 recorded — that address is SCB SHCSR; it is now symbolic. (3) The vector table was truncated at the last fill slot on an invented terminator rule; bounded by the first code address instead it is **80 slots at `0x0..0x140`** (16 core + 64 external) and **`IRQ63` → `0x00000ad0`** is recovered, so nine external interrupts are live, not eight. (4) The negatives are restated as search results with explicit reach, not proofs of absence. (5) Phase 5 is declared **first-pass**, explicitly incomplete with its exit gate only partly met. (6) Classification moved from 1 MiB block bases to **per address**: unidentified spaces are `unknown` rather than `vendor-mmio`, mixed blocks say `mixed`, and the access at `0x18037224` past the proven runtime end is no longer called RAM. (7) The recovery threshold is **31** consecutive samples, not 30. (8) The tautological handoff-window check is replaced by the image-dependent *entry record fits inside the fixed handoff window*. (9) Every register row now carries `confidence`, `kind_basis` and `initialization_values` separate from `stored_values`. (10) `uncovered_spans_aligned` is replaced by `uncovered_span_counts_equal` and `uncovered_spans_fully_compared`. Logs 100 and 101 are unedited. 382 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `103-phase4-prose-and-policy-corrections.txt` | The two log-102 commands re-run correctly; `grep`/`sed` over the regenerated Phase 4 note; `python3 tool/analyze_boot_acceptance.py` rule listing; `analyze_boot_structures.py` policy check; a register-schema census; `map_hardware_interfaces.py` confidence line; `--check` on phases 3, 4 and 5; `py_compile`; the analyzers; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check` on worktree and index | **Round-two Phase 4/5 corrections, five items.** (1) `report_phase4.py` carried its prose as a static block, so earlier fixes reached the embedded tool output but not the text: it said 30 samples, "None exist", `VTOR + 0x1c`, and stated flatly that the application runs from address zero. Regenerated — **31 samples**, scoped negatives, `*(VTOR) + 0x1c`, and execution at address zero now stated as an **inference** gated on the unresolved alias, with the two things the handoff does settle kept separate. (2) The 64 KiB entry-record fit is **downgraded from proven to builder policy**: no branch tests it and a longer record is simply truncated, so the check is renamed with a `policy:` prefix in both analyzers. (3) `reset_writes`/reset-path renamed **reset-reachable** throughout — call-graph reachability is not proof of execution during init. (4) Log 102's two `SyntaxError` commands (doubled `%%` escapes, whose NOTEs asserted results they never produced) are **re-run correctly here**; log 102 is unedited. (5) The 80-slot/IRQ63 vector reading is labelled **strongly inferred**, with its two corroborating facts and the absence of any exact IRQ-count source stated. 406 offline tests pass; both evidence hashes unchanged; **no device was accessed** |
| `104-phase5a-pointer-tables-and-reachability.txt` | `python3 tool/find_pointer_tables.py` text, `--vendor`, `--json` twice and `--seed-args`; four `FalchionSeedVectors.java` seeding runs plus four re-analyses; eight `-readOnly -noanalysis` regeneration runs; `python3 tool/map_hardware_interfaces.py` reachability; `sed` over `notes/references.md` and `notes/dual-core-question.md`; `grep` for `0x18038000` accesses; `--check` on phases 3, 4 and 5; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum` before/after plus `dumps/device/SHA256SUMS`; `git diff --check` | **Step 6 Phase 5A — Phase 5 still INCOMPLETE.** Added `tool/find_pointer_tables.py` (22 tests), `notes/references.md` and `notes/dual-core-question.md`. Ghidra cannot follow a call through a pointer table, which is why only 51 of 530 application functions were reachable. The detector reports a run of **≥3 Thumb pointers at a constant stride** targeting an even address at or above the image's first code address — the code floor matters, since without it structure words carrying bit 0 yield targets like `0x00000004`. Found **5 tables**: the entry image's `0x1404` (eight 16-byte structs all carrying one handler `0x00000a0c`) and `0x5680` (3×24-byte structs, 3 handlers), and the application's `0x18016d44` (26), `0x18017d08` (6) and `0x18018ce8` (12). **25 of the 44 application entries were already known functions**, and the vendor tables sit at exactly `-0x2c`, the relocation Phase 3 measured independently. 22 new targets seeded in both releases; function counts 97→**101** and 530→**573**, symmetric. Table entries are now reachability roots, so **application reachability rose 51 → 138 of 573** and contexts name the entry mechanism. **Not closed:** 435 still unreached; flash tables are exhausted, so the remaining mechanisms are RAM-installed callbacks, tables in the unreconstructed decompressed region, or computed dispatch — reconstructing that region is the named next step. Also recorded the owner-supplied SNC7320-series product brief as a **series-level** source that may not assign register identities (not fetched, so "no network access" holds), and answered the dual-core question: one core runs everything on the recovered path, the unexplained `0x18038000` image is the best second-core candidate, and nothing that starts it has been found. 5B–5G not started. 406 tests pass; evidence hashes unchanged; **no device was accessed** |

| `105-decompressed-region-reconstruction.txt` | `python3 tool/reconstruct_decompress.py` (text, `--json`, and `--write`); a two-release word-level diff; two `ghidra-analyzeHeadless -import` runs of the reconstructed regions plus one of a seeded pseudorandom control, into `ghidra/project-step6`; five `-process <program> -readOnly -noanalysis -postScript FalchionRegionDisassembly.java` measurement runs; `python3 tool/find_pointer_tables.py`; `python3 tool/map_hardware_interfaces.py`; `python3 tool/report_phase5.py`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all three `report_phaseN.py --check`; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries; `git diff --check` | **Step 6 Phase 5A continuation — the decompressed scatter region is reconstructed.** Added `tool/reconstruct_decompress.py` (23 fail-closed tests), `ghidra/scripts/FalchionRegionDisassembly.java`, and the two reconstructed regions under `ghidra/imports/`. The decoder is a translation of the firmware's own handler at Candidate A program `0x17c..0x1d8`, **byte-identical in both releases** (sha256 `582c4804…6ae0`), and it refuses to run against a handler that does not hash to those bytes. Both releases decode to exactly the descriptor's `0xb04`, consuming the derived stream up to word alignment with an all-zero remainder — an earlier "consumed exactly" check FAILED and was replaced by the two checks that state the real invariant, not relaxed. **What the region is was read out of its bytes**: a HID report descriptor at `+0x8`, USB string descriptors `ASUSTeK` / `ROG FALCHION ACE HFX` / `hid driver` / `Sonix HID`, and at `+0x284` the adjacent halfwords idVendor `0x0b05`, idProduct `0x1b7e`, bcdDevice `0x0159` — the identity `notes/findings.md` recorded from sysfs in log 04, which the decoder was never given; the vendor image decodes to `0x0158` at the same offset. The two independently decoded regions differ in 45 of 2,820 bytes, 35 of 39 differing words by exactly the `0x2c` Phase 3 measured. The **Thumb-2 disassembly rate is reported with a control and supports nothing**: seeded random noise 95.53%, known code 97.13–97.93%, the region 98.44–98.72% — above known code. The pointer survey was extended over the region (`CODE_CEIL`, so it may point below its own base) with the two flash surveys pinned unchanged by a regression test: **the region holds no table**, only five isolated pointers, of which three name functions Ghidra already recognises and are admitted as roots on that external agreement alone. Application reachability **138 → 146 of 573**; entry image unchanged at 91/101; function counts unchanged, so Phase 3 needed no regeneration. Recorded the required provenance: table `0x5680`'s middle entry `0x4018` was absorbed into `PtrTarget_00004004`'s body extent, so that table contributes **two** roots, not three — `reachability()` now reports a root naming no function instead of dropping it. Of the 427 unreached application functions, **135 have no caller at all** and reach the other 292; the largest unaccounted entry mechanism is `0x1800023a`, Candidate B's documented `main`, which is **not a function in the inventory at all**. 5B–5G not started; nothing committed. 433 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

| `106-phase5a-task-entries-and-new-baseline.txt` | Two `-readOnly` `FalchionDecompileTargets.java` runs on the creation primitive and its initialiser; six `FalchionTaskCreation.java` harvest/validate/indirect runs across all four programs; six `FalchionSeedVectors.java` seeding runs (write, ignored project only) plus four re-analysis passes; eight read-only `FalchionFunctionInventory.java` / `FalchionPeripheralMap.java` regeneration runs; `python3 tool/harvest_task_entries.py` text and `--json`; `python3 tool/map_hardware_interfaces.py`; `report_phase3.py`, `report_phase4.py`, `report_phase5.py` and all three `--check`; an aligned-word search of all three slices for the largest orphans' addresses; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Step 6 Phase 5A completion — argument-passed task entries recovered.** Added `ghidra/scripts/FalchionTaskCreation.java` and `tool/harvest_task_entries.py` (22 fail-closed tests). Opens with an **erratum to log 105 STEP 10**: "absent from installed_b.txt" was wrong — `1800023a` appears once, as the *exclusive* end of `FUN_180001d2`'s range; the substantive claim (no `FUNC` entry there, so traversal cannot start) stands. Also corrects an underspecified premise: log 80's decompile was of `app_candidate_b_18000000.bin` = `8fe68a13…` = the **vendor** record slice, so all its addresses are vendor addresses; the installed primitive was **derived** as `0x18012fd0` (identical body, +0x2c) rather than assumed. **The call shape was read off the primitive's own code**, not a name: `create(entry, name, stack_words, argument, priority, out_handle)` — the initialiser `FUN_18013ea0` allocates `stack<<2`, copies ≤`0x10` name bytes to TCB+`0x34`, clamps priority to `0xe` and derives ready index `0xf-priority`, and hands `(stack_top, entry, argument)` to the frame builder. Constant propagation over each **calling** function resolved **5 of 5** call sites in **both** releases (after fixing a `strd Ra,Rb,[sp,#0]` blind spot that had made every 6th argument `unknown`): **INIT_TASK** (prio 20→14 clamped, 1 KiB), **OEM_MAIN_SERVICE_TASK** (prio 10, 16 KiB), **IDLE** (prio 0, 240 B), **Tmr Svc** (prio 2, 1520 B), **usbd_wdt** (prio 1, 1 KiB); names, stacks, priorities and arguments identical across releases, entries differing by exactly 0 or the measured `+0x2c`. **OEM_MAIN_SERVICE_TASK's entry `0x499` is in Candidate A**, loaded from the literal-pool word at `0x180003b0` — the application's RTOS creates a task whose code lives in the entry image, the mirror of log 79. Incidental corroboration of log 105: INIT_TASK passes `*(0x18000394)` = decompressed-region base + `0x284` — the exact USB device-descriptor offset log 105 found from content — to `FUN_18018b70`. Function counts 101→**114** and 573→**616** per release, symmetric, Phase 3 regenerated and still 114/114 and 616/616 matched. **New reachability baseline, not an improvement — the denominators changed**: entry **101 of 114**, application **281 of 616** (was 91/101 and 146/573). Every prior root category survives and the pinned `0x4018` "roots naming no function" provenance is intact, both now unit-tested. Residue **enumerated, not eliminated** per 5A's exit gate: 121 callerless application functions (12,313 instructions), the four largest — including log 80's dispatcher `0x18001fbe` — having **zero** references and appearing in **no** aligned word of any slice; and **81 unresolved indirect branches** (entry 28 of 35, app 53 of 54), each listed by address. 5B–5G not started; nothing committed. 459 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

| `107-phase5b-usb-routing.txt` | An exact-bytes search of five preserved images for every host-captured descriptor and fragment; six `-readOnly` `FalchionDecompileTargets.java` runs (`0x18018b70`, `0x18018082`, `0x18000a70`, `0x18018bd6`, `0x18016f6e`, `0x18001fbe`, `0x18000aec`); two `-readOnly` `FalchionValueXrefs.java` runs (the descriptor table / core struct, and the vendor RX buffer); literal-pool reads; a breadth-first call-path search from the USB core to any `0x40100000` accessor; inventory string extraction for the block's accessors; `python3 tool/map_usb_routing.py` text, `--json`, `--write` and `--check`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all three `report_phaseN.py --check`; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Step 6 Phase 5B — USB ownership, endpoints and report routing.** Added `ghidra/scripts/FalchionValueXrefs.java`, `tool/map_usb_routing.py` (24 fail-closed tests) and the generated `notes/usb-routing.{md,json}`. **All five HID report descriptors are byte-identical to the host's and live only in the reconstructed region**, contiguous at `+0x008..+0x282` (68/34/182/23/327 bytes); the fifth was located structurally and its `05 59 09 01 …` / `b1 02 c0 c0` bounds match `notes/usb-descriptors.txt`. **No assembled device, config, interface, HID-class or endpoint descriptor exists in any of the five images** — and that is not the argument: `FUN_18018b70` copies a `0x8c`-byte parameter table from region+`0x284` to RAM `0x1803435c` (rejecting `bNumInterfaces` outside 1..5 with its own "USBD_HID: error Invalid struct"), and **`FUN_18018082`'s own size arithmetic** — `9 + per interface (0x12 + 7 x endpoints)` over a `0x18` stride and a `+0x16` flag byte — **reproduces the host's `wTotalLength` `0x008d` exactly**. Every table field was matched to the host: VID/PID/bcdDevice, `bNumInterfaces` 5, `bMaxPower` 500 mA, the stored `0x20` attribute bits plus the mandatory `0x80`, all six `wMaxPacketSize` values and all four IN `bInterval`s. **`0x40100000` is named the USB device controller on the firmware's own strings** — `Vector_IRQ6` contains `send usbd_ep0_Queue error` and `send usbd_irq_Queue error`, its register base literal is `0x40100018` (explaining every census target with no leftovers), it pends PendSV via ICSR, and the block's other accessors carry `USB_PM_*` and `usbd_wdt`. The core and the driver have **no static call path**: they are joined by the 5A pointer tables `0x18018ce8` / `0x18016d44` / `0x18017d08`. **Vendor channel recovered in both directions**: RX is `FUN_18016104 -> FUN_18000aec -> 0x180233a8`, a single-slot 64-byte mailbox whose byte 0 is both command and busy flag, zero-padded, `len<=4` rejected, and **packets arriving while busy are silently dropped**; TX is `dispatcher -> FUN_18000a70` (4-byte header + payload clamped to `0x3c`) `-> FUN_18018bd6(iface=1)`, bounds-checked against the same table field that produces EP `0x85`'s `wMaxPacketSize`, with three distinct error returns **that the caller ignores**. Routing map emitted with per-link confidence and `kind_basis`; **the minimal keyboard path is EP `0x00` + EP `0x81`**, everything else optional. Unresolved and stated: `bEndpointAddress` (in no image), the OUT `bInterval`, `iSerial`, the scan/media/lighting producers, and a possible mask ROM. 5C–5G not started; nothing committed. 483 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

| `108-phase5b-prose-correction.txt` | `grep` over logs 09 and 15 to establish that no raw interface-4 bytes exist; a HID item walk of the 327-byte descriptor at region+`0x13b` against lsusb's parsed listing in `notes/usb-descriptors.txt`; `python3 tool/map_usb_routing.py` text, `--write` and `--check`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all four `--check` scripts; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Phase 5B prose correction, at the reviewer's direction. Log 107 not edited.** The EP `0x0f` `kind_basis` in `tool/map_usb_routing.py` claimed interface 4's 327-byte report descriptor was "byte-identical to the host's". **Unprovable from this repository**: interface 4 was unbound on the host, so log 09 captured `hidraw0`–`hidraw3` only and log 15 holds zero raw hex lines — for interface 4 it preserves lsusb's *parsed* items, not bytes. The basis now states the real boundary: located structurally, well-formed (the item walk consumes exactly 327 bytes), parsed item sequence matching the host's, and byte-identity explicitly not checkable. **Three checks added and none weakened** (24 → 27), including the new positive result that the descriptor walks into **155 HID items every one of which matches lsusb's 155 decoded items** — an item comparison, said as such. **A second instance the review had not found**: both `FINDINGS.md` and `TIMELINE.md` carried the same overclaim in their Phase 5B lead sentences, TIMELINE's worse for citing log 09 as the source for all five; both rewritten to separate the four byte-identical interfaces from the fifth and name the evidence for each. Nine tests added (24 → 33): the basis text must not claim byte-identity, byte-identity may be claimed only where raw bytes are preserved (asserted as an invariant after a hand-listed first draft failed), no `hidraw4` exists, the 155-item match, and fail-closed item-walk tests (size field 3 means four bytes, payload past the end raises, truncation raises, kinds decode from the prefix, a missing host section raises). No Phase 5B conclusion changed. 5C not started; nothing committed. 492 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

| `109-phase5c-scan-scheduling.txt` | Two `-readOnly` `FalchionTaskCreation.java` argument-resolution runs (`0x18018bd6`, `0x18004164`); three `-readOnly` `FalchionValueXrefs.java` runs (report buffers, the service-task event word in both images, the key-state struct and per-key array); seven `-readOnly` `FalchionDecompileTargets.java` runs (`0x1800f4f2`, `0x1800f572`, `0x18004164`, `0x180061c2`, `0x180000e4`, entry `0x498`/`0x42c`/`0x4ba`, `0x1800439a`); MMIO census aggregation per block and per function for both images; breadth-first closures from the service task and from each per-tick job; literal-pool reads; `python3 tool/map_scan_pipeline.py` text, `--json`, `--write`, `--check`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all five `--check` scripts; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Step 6 Phase 5C — scan scheduling recovered; acquisition explicitly not.** Added `tool/map_scan_pipeline.py` (26 fail-closed tests) and generated `notes/scan-pipeline.{md,json}`. Opens with a **rider to log 108**: the same byte-identity overclaim survived in `map_usb_routing.py`'s module docstring, a fourth instance log 108 missed; corrected, and the phrase now appears nowhere in the repository. **The chain is closed**: `Vector_IRQ38` (the sole writer of `0x1801ee84`, the first word of the zeroinit region) → `Task_OEM_MAIN_SERVICE_TASK` (entry `0x498`), which **busy-spins** and does an **interrupt-masked** `cpsid i` / read-and-clear / `cpsie i` → `FUN_0000042c`, a **cascaded prescaler 1, 8, 5, 2, 10, 10** read off its compare constants → `FUN_000004ba` → **cross-image veneers** → `FUN_180061c2`, which both builds and sends. Constant propagation resolved every send: **interface 0 / 8 bytes / `0x1801e7c8`** (region+`0x448`) and **interface 3 / 19 bytes / `0x18023c20`**, plus the 4- and 5-byte interface-2 reports in one contiguous block — closing 5B's untraced producers. The veneer route also **resolves three of log 106's largest orphans** (`FUN_18004a7e`, `FUN_180057fe`, and `FUN_180061c2` itself). **No MMIO block was renamed**: no block in either image has a scan shape, the entry-image closure from the service task reaches 11 functions and **zero** MMIO, and the app-side per-tick jobs are dominated by unresolved-base accesses. **The pipeline bottoms out at a per-key 16-bit array** at `0x18023410+0x14` sized by `*(0x1801e734+0xc) << 1` — the shift proves halfword elements — with **no recovered producer**; consistent with Hall-effect analog sampling, marked 5D's subject, and **no contact-matrix model is asserted**. **Dimensions**: the NKRO report is **152 bits** (Report Size 1 × Report Count `0x98`) = 19 bytes, read from its own descriptor items and **explicitly not** the 189-entry translation table; rows/columns/keys are **unresolved** because the element count is a runtime value the region initialises to zero. Cadence source observed, ratios observed, **absolute period unresolved**. 5D–5G not started; nothing committed or staged. 518 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

| `110-phase5d-hall-acquisition.txt` | A HID item walk of interface 2's report descriptor accumulating bits per Report ID; four `-readOnly` `FalchionValueXrefs.java` runs (`0x180233fc`/`0x18023c0c`, `0x180344f4`, `0x18034850`, `0x1801ed6c`); three `-readOnly` `FalchionDecompileTargets.java` runs (`0x18005a88`, `0x18004a7e`) with listing greps for `#0x64`, `#0x5`, `#0x7f`, `#0x4b`, `#0xf` and the `rsb`/`add` shift-multiplies; literal-pool dereferencing from the region's initialised bytes; a 0x40000000 sub-block census by user, access width and direction; `python3 tool/model_hall_actuation.py` text, `--json`, `--write`, `--check`; `python3 tool/map_scan_pipeline.py --write`/`--check`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all six `--check` scripts; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Step 6 Phase 5D — the actuation comparison recovered as executable arithmetic; the acquisition explicitly not.** Added `tool/model_hall_actuation.py` (41 tests) and generated `notes/hall-actuation.{md,json}`. Opens with **two riders to log 109**: (1) its contiguity check printed `0x18023c33 +4 -> 0x18023c38` when `+4` gives `0x18023c37` — re-measuring interface 2's reports from its own descriptor (IDs 1/2/3/4 = 4/2/21/5 wire bytes) shows a **one-byte pad at `0x18023c37`** *and* that the 5-byte buffer log 109 called "system" is the **mouse** report (ID 4), the system report being the 2-byte buffer at `0x18023c3d` log 109 left unlabelled; (2) log 109's "per-key **halfword** array" at `0x18023410` is really the **key-state bitmap** — five 32-bit words indexed `*(uint *)(base + group*4)`, previous-state partner at `+0x810` — the halfword inference does not survive. **The decision is recovered and listing-confirmed**: `cmp r3,#0x64` at `0x180057b4` → **travel ≥ 100 = down, == 0 = up, 1..99 = UNCHANGED**, a hold band read from control flow, not a feature name; key ids `0x00`/`0xd3` skipped. **The geometry is in the instruction encoding** — `rsb ...,lsl #4` then `add ...,lsl #2` is ×15 then ×5 = **5 groups × 15 = 75 entries per layer** — the first physical-side dimension this project has proved; the *active* per-group count is a runtime word the region initialises to zero, recorded as a boundary. Per-key config: 7-bit field at `+8` of a `0x20` record in `0x180202d8 + profile*0xd84`, bit 15 selecting per-key over a global, clamped `<2→0, 2..4→v-2, ≥5→3`; consistent with FINDINGS' `0x51/0x22` "bytes 7-8 ÷ 10" but the write path was **not traced**, so only consistency is claimed. **No ADC block exists**: every `0x40000000` access is 32-bit across all four sub-blocks; `0x40022000` has a timer/PWM shape and is **left unnamed**. The travel buffer's address appears in **no** aligned word of any image — reachable only through the pointer cell `0x1801ed6c` — so **acquisition, calibration, filtering, raw-to-travel and fault behaviour are all unrecovered**, each with its reason; no emulation was done and why is stated. A check caught a bad relocation rule of mine: `0x18004a7e` and `0x18005a88` both sit above the insertion yet relocate by 0 and `0x2c`, so counterparts are now **looked up** in Phase 3's match table, which also records `FUN_18004a7e`'s match as only **tentative**. Physical units, polarity, voltage, noise and safe rate are **not established and not establishable**; a test asserts no number carries a unit. **Authorises no Hall drive and no live experiment.** 5E–5G not started; nothing committed or staged. 560 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

| `111-phase5e-nonvolatile-settings.txt` | Listing greps of the dispatcher for the opcode/subcommand compare trees; four `-readOnly` `FalchionValueXrefs.java` runs (`0x18022c60`, `0x18025ef4`, and the literal-pool cells); five `-readOnly` `FalchionDecompileTargets.java` runs (`0x18001fbe`, `0x18000d56`, `0x1800e272`/`e2a8`/`e2c4`, `0x18011dd0`); literal-pool dereferencing from raw bytes; a per-function MMIO census of the whole `0x1800e2xx-0x1800efxx` storage cluster; a call-graph closure from `FUN_1800dc92`; `python3 tool/map_nonvolatile_writes.py` text, `--json`, `--write`, `--check`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all seven `--check` scripts; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Step 6 Phase 5E — the commit path traced statically; no command constructed.** Added `tool/map_nonvolatile_writes.py` (27 fail-closed tests) and generated `notes/nonvolatile-writes.{md,json}`. **The answer is (b): the commit QUEUES.** The `0x50/0x55` branch at `0x18002428` is nine instructions and **contains no call at all** — it sets a command byte `*(0x18022ce4) = 4` (`strb.w r12` with `mov.w r12,#0x4`), guarded so a repeat is idempotent; the neighbouring `0x60` branch *does* call, so the absence is a property of the branch. Two further hops follow: `FUN_18000d56` switches on that byte and calls `FUN_1800e2a8(0x320000)` / `FUN_1800e2c4(0x330000, 0x340000)` and a computed `0x320000 + byte*0x4000`; those fill a **one-deep request struct** at `0x18025ef4` (opcode `+0`, address `+0xc`, pending `+0x18`) with opcode **`0xd8`/`0x52`**; `FUN_1800dc92` drains it and reaches hardware only five calls later at `FUN_18011dd0`, a **DMA setup** bounds-checking its source against `0x18000000` and touching `0x40020008-0x4002001c` and `0x45000000/0c/54`. **No function in the 36-strong storage cluster touches MMIO.** The opcodes match the JEDEC SPI-NOR block-erase codes — recorded as **recognition, not proof**; the medium stays **UNIDENTIFIED** and **no block is named**. **The settings format was not recovered** (no magic, version, length or checksum is constructed or verified anywhere on the path), and the `0x51/0x22` → per-key-bank link is **still open**: the bank pointer is loaded in the `0x60` branch, not `0x22`. Target ranges start at `0x320000` and are **disjoint from the bootloader's `0x10000..0x7c000`** application region. **Exit gate: the omit-all-writes branch, explicitly** — safe persistence cannot be implemented from this evidence, but the negative is provable in four steps, and the residual is stated: the proof holds only *within the traced set*, and both the state machine and the drainer are callerless. 5F/5G not started; nothing committed or staged. 587 offline tests pass; both evidence hashes unchanged; **no device was accessed and no write command was constructed** |

| `112-phase5f-rgb-lamparray.txt` | An item walk of interface 4's 327-byte descriptor; six `-readOnly` `FalchionDecompileTargets.java` runs (`0x180184b6`, `0x18008f4a`, `0x18008f12`, `0x1800ffaa`, `0x18010102`, `0x1800c132`); two `-readOnly` `FalchionValueXrefs.java` runs (the frame buffer, the lamp table pointer); one `FalchionTaskCreation.java` argument resolution on `0x18018c76`; literal-pool dereferencing from raw bytes of both the app and entry slices; call-graph MMIO closures for the frame writers and consumers; `python3 tool/map_rgb_lamparray.py` text, `--json`, `--write`, `--check`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all eight `--check` scripts; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Step 6 Phase 5F — the LampArray route recovered end to end; the LED driver not.** Added `tool/map_rgb_lamparray.py` (30 tests) and generated `notes/rgb-lamparray.{md,json}`. **Interface 4 declares NO Output report** — all thirteen Main items are Feature — so the LampArray runs over **control transfers** and EP `0x0f` carries none of it, correcting 5B's framing. Route: `FUN_180184b6` (`0xa101` GET / `0x2109` SET, type 3, 64-byte cap) → an indirect call through `*(0x1801ebb8)` → **`FUN_18008f12`**, installed by `FUN_18008f4a` during INIT_TASK, which dispatches on **bRequest** (1→GET `FUN_1800ffaa`, 9→SET `FUN_18010102`). **All six reports are handled** (1,3 GET; 2,4,5,6 SET). The GET handler returns `0x17` = 23 bytes for report 1 — **exactly** the item walk's 22+1, derived twice independently. Lamps are addressed **by LampId through a 2-byte coordinate table**, each bounds-checked against the active lamp count. **Frame buffer `0x1802505e`: 6 rows × 17 columns × 3 bytes = 306 bytes**, order **R,G,B**, 8 bits each, intensity as `(channel × intensity) >> 8`, out-of-range cells **dropped silently** — all read off `FUN_1800c132`'s listing. **LED counts from evidence**: a table in the **entry image** at `0x500c` gives eight configurations of 23–89 lamps, every one ≤ the 102 cells. **The final hardware interface is UNRESOLVED**: both frame consumers reach zero resolved MMIO (79 and 91 unresolved-base accesses); SPI/PWM/GPIO/DMA all open, no link to `FUN_18011dd0`, and `0x40022000` is **not named** despite a PWM-ish shape. **Classified IMPLEMENTED with a ceiling** — the host-facing protocol is fully reproducible but a replacement could satisfy a LampArray host and light nothing. **Safe omission answered in two halves**: the buffer's all-zero idle state is **provable** (zeroinit region, single writer), the LEDs' is **not** (driver polarity unknown). Records a misreading I made and corrected mid-phase: `FUN_18008f12`'s first argument is the report ID, not the interface — settled by reading the call site rather than the callee's signature. 5G not started; nothing committed or staged. 617 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

| `113-phase5g-and-final-dependency-map.txt` | Per-function MMIO censuses over both peripheral maps filtered to the magic-key blocks; call-graph closures for the `usbd_wdt` task body; three `-readOnly` `FalchionDecompileTargets.java` runs (entry `0x1216`/`0x20be`, app `0x1800023a`, entry `0x1f50`/`0x1930`); one `FalchionTaskCreation.java` `validate=` run on the start routine; two `-readOnly` `FalchionValueXrefs.java` runs; exhaustive aligned-word searches across all five preserved images for the second-image descriptor pair and the handshake token; literal-pool dereferencing from raw bytes; manual decode of the veneer's `movw`; `python3 tool/map_platform_dependencies.py` text, `--json`, `--write`, `--check`; `py_compile`; `python3 -m unittest discover -s "$PWD/tool" -t "$PWD/tool"`; all nine `--check` scripts; `sha256sum -c logs/SHA256SUMS`; `sha256sum` on both evidence binaries before and after; `git diff --check` | **Step 6 Phase 5G + the Phase 5 FINAL DEPENDENCY GATE.** Added `tool/map_platform_dependencies.py` (33 tests) and generated `notes/platform-dependencies.{md,json}`, which reads the five 5B–5F models rather than restating them. **Watchdogs**: exactly **one** function in either image touches `0x40008000`/`0x40009000` — `FUN_00001216` on the reset path — writing key `0x5afa55aa` to `+0xc` then `0x5afa0000` to `+0`; **nothing feeds them anywhere**. Recorded as inference from the write pattern, **not identification**. **The `usbd_wdt` lead does NOT pan out**: its 12-function closure reaches only ICSR and no watchdog block — a software supervisor, not a feeder. **Clocks**: the reset path faults out unless MSP lies in `0x18000000..0x18040000` (a hard listing-verified constraint) and runs a fixed five-call chain; **no frequency is claimed** and a test forbids any Hz figure. **Faults**: NMI writes `VECTKEY|SYSRESETREQ` (a hardware action → must-neutralize); HardFault reads CFSR/MMFAR/BFAR only (diagnostic); the 80-slot vector extent stays **strongly-inferred** by test. **Multicore — log 104's open question is answered**: `CandidateB_Main` hands `0x60074000` to entry-image `0x1f50` via a veneer (`movw r12,#0x1f51`, hand-decoded; the target validates as a subroutine and manipulates `0x45000100`), then **spins** until `0x20000000` holds `0x12345678` — a token that appears in exactly two places repository-wide, one of them **inside the `0x18038000` image at +0x3214**. A second execution context with a mailbox handshake. **What it owns stays unresolved** and is a *candidate*, not a finding. **Final gate**: 17 services partitioned **6 must-implement / 3 must-neutralize / 5 may-omit / 3 unresolved**, every claim cited to a log step. The gate's teeth are enforced by tests — an unresolved service must name a >60-char evidence boundary, may-omit requires a **proven** safe idle state, and its contrapositive makes **RGB a blocker** despite a fully recovered protocol. **Prototype: 3 of 5 requirements done, 1 partial, 1 blocked** — the **Hall acquisition is the largest blocker**, with the second context as the leading (untested) hypothesis. No MMIO block named; the `0x18038000` image was **not** imported or analysed. Nothing committed or staged. 650 offline tests pass; both evidence hashes unchanged; **no device was accessed** |

Repository/context commands also run: `pwd`, `rg --files -g AGENTS.md`, `rg --files`, `git status --short`, `sed` on existing Falchion notes, `mkdir -p logs`, `diff` on prior captures, and `sha256sum logs/*.txt`. Reading the newly created logs with `sed`, `wc`, and `tail` did not access the USB device.

Later offline-analysis setup also checked local tool availability and created raw
payload/ELF wrappers only under `/tmp`. One initial zsh loop passed an empty offset
to `xxd` and decoded the firmware header instead of the intended slices; it made
no changes and was immediately corrected with `name:offset` loop values.
